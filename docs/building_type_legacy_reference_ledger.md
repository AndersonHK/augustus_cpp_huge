# Building Type Legacy Reference Ledger

This ledger tracks legacy code paths that still refer to specific building type ids. Entries here are the cleanup queue for future phases that should replace type-specific logic with BuildingType properties, tags, policies, or registry lookups. Hard legacy save migration is the main intentional exception.

For the detailed save/load bridge that maps save-local building type ids through text ids, runtime ids, legacy structs, monument construction definitions, and runtime wrappers, see `docs/save_load_runtime_bridges.md`.

Status values:

- `migrated`: no longer depends on stable legacy enum identity.
- `bridged`: still touches legacy-specific behavior, but runtime/save identity goes through the BuildingType id bridge.
- `legacy-retained`: intentionally kept as old-save compatibility or hard legacy behavior for now.
- `needs-future-phase`: observed legacy authority that should move to BuildingType data.

## Entries

| Location | Building id(s) | Why the code cares | Future replacement | Status |
| --- | --- | --- | --- | --- |
| `src/building/state.cpp::building_state_save_to_buffer` | all building types | Save records previously wrote `building.type` as the raw legacy enum slot. | Save-local `building_type_table` id resolved through `building_type_id_bridge`. | migrated |
| `src/building/state.cpp::building_state_load_from_buffer` | all building types | Save records previously restored `building.type` directly from the numeric legacy slot. | Load save-local ids through the active runtime table; old saves go through `building_type_legacy_migration`. | migrated |
| `src/building/state.cpp::write_type_data` and `read_type_data` | rubble/original building type fields | Rubble and warehouse-space records store the original building type as a numeric id. | Save/load `og_type` through the same save-local/runtime bridge. | migrated |
| `src/building/state.cpp::read_type_data` | `BUILDING_CARAVANSERAI`, `BUILDING_LARGE_TEMPLE_CERES`, `BUILDING_LARGE_TEMPLE_VENUS`, `BUILDING_GRANARY`, `BUILDING_WAREHOUSE`, `BUILDING_DEPOT`, `BUILDING_DOCK`, `BUILDING_WHARF`, `BUILDING_BURNING_RUIN`, `BUILDING_WAREHOUSE_SPACE`, farm/workshop ranges | Type-specific save payload decoding and old-save compatibility. | Per-type save payload policy or component schema; legacy-only branches stay in compatibility code. | bridged |
| `src/building/state.cpp::building_state_load_from_buffer` compatibility tail | forts, monuments, temples, arches, gatehouses, barracks, grand temple Mars, lighthouse, caravanserai, colosseum, hippodrome, wheat farm | Old-save repairs and type-specific post-load fixes. | Move true legacy repairs into save migration helpers; replace ongoing behavior with tags such as `is_fort`, `monument_kind`, `accepts_weapons`, `industry_category`. | bridged |
| `src/building/building_type_registry_xml.cpp::find_building_type_by_attr` | XML-owned building types | XML type ids now resolve through the explicit legacy migration table for enum-retained buildings, while theater/well allocate runtime ids dynamically. Non-XML legacy definitions still fall back to `building_properties.event_data.attr`. | Remove the enum-retained XML mappings as each remaining static building reference is migrated to cached runtime ids or properties/tags. | bridged |
| `src/building/building_type_registry_xml.cpp::parse_definition_file` | metadata-only BuildingType XML | Parser temporarily accepts metadata-only definitions during the build-authority migration. This is not the desired long-term validation behavior. | Restore strict load-time errors for incomplete live XML once XML owns graphics/runtime/placement/menu authority. | needs-future-phase |
| `src/building/building_type_id_bridge.cpp` | all legacy enum slots | Transitional runtime/save bridge still seeds fallback text ids from hardcoded legacy properties. | Runtime table should be generated from active XML plus explicit fallback definitions only. | bridged |
| `src/building/building_type_legacy_migration.cpp::build_legacy_text_ids` | `BUILDING_FORT_GROUND`, `BUILDING_DISTRIBUTION_CENTER_UNUSED`, `BUILDING_WAREHOUSE_SPACE`, `BUILDING_BURNING_RUIN` | Persistent internal building records do not have legacy XML/event attr names, but still need save-local text ids for the new bridge. | Explicit compatibility table for old save ids plus registry-owned definitions for internal/runtime-only building parts. | legacy-retained |
| `src/building/building_type_registry.cpp::building_type_registry_apply_model_overrides` | XML-loaded building types with `<model>` or `<labor>` | Legacy model storage still receives cost/desirability/labor values by legacy runtime id after XML load. | Registry-sized runtime model storage keyed by generated BuildingType runtime id. | bridged |
| `src/game/file_io.cpp::savegame_load_from_state` | XML-loaded building types with model overrides | Savegame model data still exists and can carry legacy values; XML overrides now reapply after loading that data. | Remove or migrate saved model data once BuildingType XML is authoritative for build models. | bridged |
| `src/building/building_type_registry_xml.cpp::parse_model` | XML-loaded building types with `size` | XML can now parse footprint size, but legacy placement still reads hardcoded `building_properties.size`. | Foundation/placement pass should consume BuildingType model size directly. | needs-future-phase |
| `src/building/building_type_legacy_migration.cpp` | XML-owned old save enum slots | Old saves without `building_type_table` need enum-to-text migration for buildings whose model/size now live in XML. | Keep this table as compatibility-only; remove entries only when old-save migration no longer needs the legacy enum slot. | legacy-retained |
| `src/building/menu.cpp` | hardcoded menu groups and entries | Build menu membership and order are still hardcoded by enum for legacy-only items and submenu expanders; XML-owned entries with `<button>` data are now filtered out of the legacy arrays and rebuilt from the registry catalog. | BuildingType `<button>` data: `group`, `order`, `icon`, and localized display key for all buildable entries, with legacy-only submenu expanders retired or modeled explicitly. | bridged |
| `src/building/properties.c` | XML-owned building rows | Static property rows for XML-owned buildings have been removed. Runtime property fields still read by legacy systems, such as size, sound, event attr, fireproof, desirability-range drawing, and Venus bonus, are applied from BuildingType XML at registry load. | Remove the legacy `building_properties` runtime bridge once all callers read these fields directly from BuildingType definitions. | bridged |
| `src/building/properties.c` | `BUILDING_CLAY_PIT`, `BUILDING_MARBLE_QUARRY`, `BUILDING_IRON_MINE`, `BUILDING_TIMBER_YARD`, `BUILDING_GOLD_MINE`, `BUILDING_STONE_QUARRY`, `BUILDING_SAND_PIT` | Raw-material producer static rows were removed. Runtime property fields now come from BuildingType XML and the registry model/property override bridge. | Remove the registry-to-legacy property bridge once all remaining callers read BuildingType definitions directly. | migrated |
| `src/building/construction.cpp::set_required_terrain` | raw-material producers | Raw-material producer terrain gates are now authored as BuildingType `<foundation><terrain ... /></foundation>` data. The function still contains legacy fallback cases for farms, towers, and lighthouse. | Move the remaining terrain fallback cases into BuildingType XML before considering `set_required_terrain` for deletion. | bridged |
| `src/building/industry.cpp` | raw-material producers | Raw-material producers now attach native one-output `ProductionMethod` XML and output `StorageType` XML; gold mines preserve their legacy 600-denarii per-cycle charge through `<treasury_cost amount="600" />`. The legacy industry branch remains until the runtime can fully route these buildings through native production in all modes/save states. | Confirm native production coverage for raw producers, then trim the matching legacy industry branches. | bridged |
| `src/building/production_method.*` and `src/building/production.cpp` | `BUILDING_GOLD_MINE` / `gold_mine` | Gold mine production has a legacy treasury side effect at the start of each production cycle. | The `treasury_cost` ProductionMethod field now models that behavior directly; keep the field generic for future money-consuming producers. | migrated |
| `src/building/image.cpp` | raw-material producers | Raw-material producer image switch cases were removed. `building_image_get` now asks BuildingType XML graphics for an asset id before falling back to legacy image cases. | Continue trimming XML-owned image cases after each BuildingType graphics definition has a verified payload target. | migrated |
| `src/building/menu.cpp` | raw-material producers | Raw-material producer submenu membership now comes from BuildingType `<button group="raw_materials" ... />`. Tutorial unlock logic enables the generated submenu entries instead of naming each raw producer. | Move remaining hardcoded submenu contents into BuildingType button data. | migrated |
| `src/building/count.c` | raw-material producers | Raw-material producer counting now iterates resource metadata for raw-material industries instead of a hardcoded raw-producer type array. | Resource metadata remains the authority until resource/industry ownership migrates further. | migrated |
| `src/city/labor.cpp` | raw-material producers | Raw-material producer labor category assignment now iterates resource metadata for raw-material industries instead of naming each producer. | Move labor categories into BuildingType XML if broader labor-category data ownership is needed. | migrated |
| `src/figure/roamer_preview.c` | raw-material producers | Roamer preview now uses BuildingType labor-seeker metadata for XML-owned labor seekers before falling back to legacy cases. Raw-material producer cases were removed. | Keep moving labor-seeker preview behavior behind BuildingType metadata. | migrated |
| `src/core/lang.cpp` | XML-owned legacy enum buildings | Building name lookup now checks localized BuildingType `identity name_key` values before the legacy switch. The temporary theater/display-key fallback was removed, and stable XML-owned cases were deleted for raw-material producers, small temples, grand temples, Pantheon, architect guild, ponds, lighthouse, tavern, arena, watchtower, brickworks, concrete maker, armoury, caravanserai, and workcamp. The switch still has other legacy cases, so the function is not yet a deletion candidate. | Continue deleting switch cases as each building gets stable name/button XML with real localization keys. | bridged |
| `src/sound/city.cpp::sound_city_mark_building_view` | theater, entertainment venues, governor residences, native buildings, water-gated buildings | City sound muting during enemy attacks, always-play behavior, and reservoir/water-access muting used to be hardcoded by building type. | BuildingType `<sound>` flags plus BuildingType water access rules; sound-only XML entries bridge legacy-only buildings until their full BuildingType data is migrated. | bridged |
| `src/building/industry.cpp::building_industry_update_production` | `BUILDING_CONCRETE_MAKER` | Concrete production used a type-specific water access progress multiplier. | Native ProductionMethod now asks the owning BuildingType whether water access is required and refuses to produce when the building lacks access; future production tuning should be data-driven if concrete needs more than binary water access. | migrated |
| `src/building/construction.cpp` and `src/building/construction_building.cpp` | many specific construction types and ranges | Placement, foundations, road/water/monument rules, and special creation flows are type-specific. | BuildingType `FoundationDefinition`, footprint, road access, water access, monument/build components. | needs-future-phase |
| `src/window/build_menu.cpp` and `src/widget/sidebar/city.cpp` | build-menu display ids and availability checks | Build-menu rows now use BuildingType `button text_key` when it resolves to a registered project localization key, then fall back to the legacy group/id string path for entries that still use placeholder `building.*.name` keys. Costs, visibility, and some buttons remain legacy-menu driven. | Generated menu catalog from BuildingType definitions and generic building button class; replace remaining placeholder button keys as matching localization keys are added. | needs-future-phase |
| `src/window/build_menu.cpp::BuildMenuButton` | visible build menu entries | City build menu rows now have an object that owns type, display text, cost/icon facts, widget binding, and activation, but it still fills itself from legacy `building_menu_*` APIs. | Populate `BuildMenuButton` instances from BuildingType `<button>` data and registry-provided build model/foundation policies. | bridged |
| `src/building/menu.cpp::menu_tool_type_for_definition` | `BUILDING_RESERVOIR`, `BUILDING_DRAGGABLE_RESERVOIR` | Reservoir XML owns the water-menu button, but the legacy build action is still the draggable reservoir/aqueduct tool rather than the saved reservoir building type. | Button action/foundation policy should be explicit BuildingType data, so one XML button can target a placement tool or placement behavior without hardcoded type translation. | bridged |
| `src/graphics/generic_button.h` and `src/graphics/generic_button.cpp` | all generic UI buttons | Shared low-level button hit testing/action dispatch is now a C++ `GenericButton` class with context data, while C callers still see a compatible struct during migration. | Move remaining C windows to object-owned button models where each UI button exposes name, visuals, widget bounds, and action without `parameter1`/`parameter2` decoding. | bridged |
| `src/building/building.c::building_load_state` | all loaded building records | Building manager rebuilds linked lists after load and should eventually prune missing BuildingType instances from map/list/storage references. | Post-load cleanup pass driven by bridge missing-id diagnostics and registry validity checks. | needs-future-phase |
| `src/scenario/event/parameter_city.cpp::building_coverage` | `BUILDING_THEATER` | Scenario event service-coverage checks still need the theater advisor coverage value for legacy event parameters. | Coverage event parameters should resolve dynamic BuildingType ids through scenario parameter serialization or a future service-category property. | bridged |
