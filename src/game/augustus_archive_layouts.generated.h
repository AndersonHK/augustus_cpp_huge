// Generated by tools/generate_augustus_archive_layouts.py; upstream GPL source attribution is retained in the inventory.
#pragma once
#include <cstdint>
#include <cstddef>
namespace augustus_archive_layouts {

// Producer caa61f5ceca8cf19e1caa785e9af2a55859c52cb; layout be7e5e02275131e6ce7f99d85e26185dbf3fed1b26e704463cc09140d1afb9ea
namespace layout_0 {
enum {

    SAVE_GAME_CURRENT_VERSION = 0xaf,

    SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION = 0x66,
    SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION = 0x76,
    SAVE_GAME_LAST_NO_DELIVERIES_VERSION = 0x77,
    SAVE_GAME_LAST_STATIC_VERSION = 0x78,
    SAVE_GAME_LAST_JOINED_IMPORT_EXPORT_VERSION = 0x79,
    SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION = 0x80,
    SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION = 0x81,
    SAVE_GAME_LAST_STORED_IMAGE_IDS = 0x83,
    // SAVE_GAME_INCREASE_GRANARY_CAPACITY shall be updated if we decide to change granary capacity again.
    SAVE_GAME_INCREASE_GRANARY_CAPACITY = 0x85,
    SAVE_GAME_ROADBLOCK_DATA_MOVED_FROM_SUBTYPE = 0x86,
    SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION = 0x86,
    SAVE_GAME_LAST_CARAVANSERAI_WRONG_OFFSET = 0x87,
    SAVE_GAME_LAST_ZIP_COMPRESSION = 0x88,
    SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG = 0x89,
    SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST = 0x8a,
    // SAVE_GAME_LAST_WITHOUT_HIGHWAYS = 0x8b, no actual changes to how games are saved. Crudelios just wants this here 
    SAVE_GAME_LAST_UNVERSIONED_SCENARIOS = 0x8c,
    SAVE_GAME_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 0x8d,
    // the difference between this version and UNVERSIONED_SCENARIOS above is this one actually saves the scenario version
    // in the data, whereas the previous one did a lookup based on the save version
    SAVE_GAME_LAST_NO_SCENARIO_VERSION = 0x8e,
    SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA = 0x8f,
    SAVE_GAME_LAST_STATIC_RESOURCES = 0x90,
    SAVE_GAME_LAST_GLOBAL_BUILDING_INFO = 0x91,
    SAVE_GAME_LAST_NO_GOLD_AND_MINTING = 0x92,
    SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS = 0x93,
    SAVE_GAME_LAST_NO_EXTENDED_REQUESTS = 0x94,
    SAVE_GAME_LAST_NO_EVENTS = 0x95,
    SAVE_GAME_LAST_NO_CUSTOM_MESSAGES = 0x96,
    SAVE_GAME_LAST_NO_CART_DEPOT = 0x97,
    SAVE_GAME_LAST_NO_NEW_MONUMENT_RESOURCES = 0x98,
    SAVE_GAME_LAST_MONUMENT_TYPE_DATA = 0x99,
    SAVE_GAME_LAST_NO_CUSTOM_VARIABLES = 0x9a,
    SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET = 0x9b,
    SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 0x9c,
    SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS = 0x9d,
    SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA = 0x9e,
    SAVE_GAME_LAST_NO_LATRINES = 0x9f,
    SAVE_GAME_LAST_SPRITE_BRIDGES = 0xa0,
    SAVE_GAME_LAST_SPRITE_BRIDGES_MIGRATION_FIX = 0xa1,
    SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS = 0xa2,
    SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS = 0xa3,
    SAVE_GAME_LAST_STORAGE_STATE_AND_QUANTITY_TOGETHER = 0xa4,
    SAVE_GAME_LAST_10_LEGIONS_MAX = 0xa5,
    SAVE_GAME_LAST_GRANARY_WAREHOUSE_NON_ROADBLOCKS = 0xa6,
    SAVE_GAME_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 0xa7,
    SAVE_GAME_LAST_U16_GRIDS = 0xa8,
    SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA = 0xa9,
    SAVE_GAME_LAST_STATIC_PATHS_AND_ROUTES = 0xaa,
    SAVE_GAME_LAST_NO_EMPIRE_EDITOR = 0xab,
    SAVE_GAME_LAST_LIMITED_ROUTE_COST = 0xac,
    SAVE_GAME_LAST_NO_WATER_IN_ADVISORS = 0xad,
    SAVE_GAME_LAST_NO_SHARED_BUILDINGS = 0xae
};
enum {
    SCENARIO_CURRENT_VERSION = 22,

    SCENARIO_VERSION_NONE = 0,
    SCENARIO_LAST_UNVERSIONED = 1,
    SCENARIO_LAST_NO_WAGE_LIMITS = 2,
    SCENARIO_LAST_EMPIRE_OBJECT_BUFFERS = 3,
    SCENARIO_LAST_EMPIRE_RESOURCES_U8 = 4,
    SCENARIO_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 5,
    SCENARIO_LAST_NO_SAVE_VERSION_WRITE = 6,
    SCENARIO_LAST_NO_STATIC_RESOURCES = 7,
    SCENARIO_LAST_NO_DYNAMIC_OBJECTS = 8,
    SCENARIO_LAST_NO_EXTENDED_REQUESTS = 9,
    SCENARIO_LAST_NO_EVENTS = 10,
    SCENARIO_LAST_NO_CUSTOM_MESSAGES = 11,
    SCENARIO_LAST_NO_CUSTOM_VARIABLES = 12,
    SCENARIO_LAST_WRONG_END_OFFSET = 13,
    SCENARIO_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 14,
    SCENARIO_LAST_STATIC_ORIGINAL_DATA = 15,
    SCENARIO_LAST_NO_ALT_NATIVE_HUTS = 16,
    SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS = 17,
    SCENARIO_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 18,
    SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA = 19,
    SCENARIO_LAST_NO_EMPIRE_EDITOR = 20,
    SCENARIO_LAST_LIMITED_ROUTE_COST = 21
};
enum {
    SAVEGAME_STATUS_NEWER_VERSION = -1,
    SAVEGAME_STATUS_INVALID = 0,
    SAVEGAME_STATUS_OK = 1
};
typedef struct {
    struct {
        int burning_totals;
        int image_grid;
        int building_grid;
        int rubble_grid;
        int terrain_grid;
        int figures;
        int route_figures;
        int route_paths;
        int formations;
        int buildings;
        int building_list_burning;
        int building_list_small;
        int building_list_large;
        int building_storages;
        int monument_deliveries;
        int enemy_armies;
        int scenario;
        int graph_order;
        int city_data;
        int empire_cities;
        int trade_prices;
        int trade_route_limit;
        int trade_route_traded;
        int figure_traders;
        int invasion_warnings;
    } piece_sizes;
    struct {
        int culture1;
        int culture2;
        int culture3;
        int military;
        int industry;
        int support;
    } building_counts;
    struct {
        int image_grid;
        int monument_deliveries;
        int barracks_tower_sentry_request;
        int custom_empires;
        int custom_empire_map_image;
        int scenario_version;
        int requests;
        int scenario_events;
        int scenario_conditions;
        int scenario_actions;
        int scenario_formulas;
        int custom_messages_and_media;
        int city_faction_info;
        int resource_version;
        int static_building_counts;
        int visited_buildings;
        int custom_campaigns;
        int dynamic_scenario_objects;
        int custom_model_data;
        int rubble_grid;
        int custom_production_rates;
    } features;
} savegame_version_data;
static int scenario_size(int savegame_version)
{
struct {
    size_t size;
    size_t start_info;
    size_t original_requests_part1;
    size_t original_invasions_part1;
    size_t start_funds_and_enemy_id;
    size_t map_size;
    size_t briefing;
    size_t original_requests_part2;
    size_t image;
    size_t herds;
    size_t original_demand_changes;
    size_t original_price_changes;
    size_t gladiator_revolt;
    size_t emperor_change;
    size_t random_events;
    size_t fishing;
    size_t original_requests_part3;
    size_t original_invasions_part2;
    size_t original_requests_part4;
    size_t rome_wheat;
    size_t allowed_buildings;
    size_t win_criteria;
    size_t map_points;
    size_t invasion_points;
    size_t misc;
    size_t alt_huts;
    size_t native_buildings;
    size_t introduction;
    size_t custom_variables;
    size_t custom_name;
    size_t end;

    // Cache info
    int version;
} buffer_offsets{};
auto calculate_buffer_offsets = [&](int scenario_version) {
    
    size_t next_start_offset = 0;

    if (scenario_version > SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.size = next_start_offset;
        next_start_offset = buffer_offsets.size + sizeof(int32_t);
    }

    buffer_offsets.start_info = next_start_offset;
    next_start_offset = buffer_offsets.start_info + 14;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part1 + 20 * 8;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part1 + 20 * 10;
    }

    buffer_offsets.start_funds_and_enemy_id = next_start_offset;
    next_start_offset = buffer_offsets.start_funds_and_enemy_id + 14;

    buffer_offsets.map_size = next_start_offset;
    next_start_offset = buffer_offsets.map_size + 16;

    buffer_offsets.briefing = next_start_offset;
    next_start_offset = buffer_offsets.briefing + 64 + 522;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part2 + 20;
    }

    buffer_offsets.image = next_start_offset;
    next_start_offset = buffer_offsets.image + 6;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 2;// favour reset monthly pattern is +2 bytes
    }
    buffer_offsets.herds = next_start_offset;
    next_start_offset = buffer_offsets.herds + 4 * 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_demand_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_demand_changes + 20 * 9;

        buffer_offsets.original_price_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_price_changes + 20 * 6;
    }

    buffer_offsets.gladiator_revolt = next_start_offset;
    next_start_offset = buffer_offsets.gladiator_revolt + 8;

    buffer_offsets.emperor_change = next_start_offset;
    next_start_offset = buffer_offsets.emperor_change + 8;

    buffer_offsets.random_events = next_start_offset;
    next_start_offset = buffer_offsets.random_events + 36;

    buffer_offsets.fishing = next_start_offset;
    next_start_offset = buffer_offsets.fishing + 8 * 4;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part3 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part3 + 20;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part2 + 20 * 1;
    }

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part4 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part4 + 20 * 4;
    }

    buffer_offsets.rome_wheat = next_start_offset;
    next_start_offset = buffer_offsets.rome_wheat + 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.allowed_buildings = next_start_offset;
        next_start_offset = buffer_offsets.allowed_buildings + 50 * 2;
    }

    buffer_offsets.win_criteria = next_start_offset;
    next_start_offset = buffer_offsets.win_criteria + 52;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 1;// earthkek pattern is +1 byte
    }
    buffer_offsets.map_points = next_start_offset;
    next_start_offset = buffer_offsets.map_points + 12;

    buffer_offsets.invasion_points = next_start_offset;
    next_start_offset = buffer_offsets.invasion_points + 8 * 4;

    buffer_offsets.misc = next_start_offset;
    next_start_offset = buffer_offsets.misc + 51;

    if (scenario_version > SCENARIO_LAST_NO_ALT_NATIVE_HUTS) {
        buffer_offsets.alt_huts = next_start_offset;
        next_start_offset = buffer_offsets.alt_huts + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        buffer_offsets.native_buildings = next_start_offset;
        next_start_offset = buffer_offsets.native_buildings + 12;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_MESSAGES) {
        buffer_offsets.introduction = next_start_offset;
        next_start_offset = buffer_offsets.introduction + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_VARIABLES && scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.custom_variables = next_start_offset;
        next_start_offset = buffer_offsets.custom_variables + 100 * 9;
    }

    buffer_offsets.custom_name = next_start_offset;
    next_start_offset = buffer_offsets.custom_name +
        (scenario_version > SCENARIO_LAST_WRONG_END_OFFSET ? 300 : 50);

    buffer_offsets.end = next_start_offset + 1;

    buffer_offsets.version = scenario_version;

};
    if (savegame_version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        return 1720;
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTENDED_REQUESTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTENDED_REQUESTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_MESSAGES);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_VARIABLES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_VARIABLES);
    } else if (savegame_version <= SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET) {
        calculate_buffer_offsets(SCENARIO_LAST_WRONG_END_OFFSET);
    } else if (savegame_version <= SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_STATIC_ORIGINAL_DATA);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_ALT_NATIVE_HUTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA);
    } else {
        calculate_buffer_offsets(SCENARIO_CURRENT_VERSION);
    }
    return (int) buffer_offsets.end;
}
static void get_version_data(savegame_version_data *version_data, int version, int resources, int foods)
{
    int multiplier = 1;
    int count_multiplier = 1;

    int total_new_resources = resources - 16;
    int total_new_food = foods - 7;

    version_data->piece_sizes.burning_totals = 8;
    if (version > SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION) {
        multiplier = 5;
    }
    if (version > SAVE_GAME_LAST_STATIC_VERSION) {
        multiplier = 0;
        version_data->piece_sizes.burning_totals = 4;
    }

    if (version > SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION) {
        count_multiplier = 0;
    }

    version_data->piece_sizes.image_grid = 52488 * (version > SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION ? 2 : 1);
    version_data->piece_sizes.building_grid = 52488 * (version > SAVE_GAME_LAST_U16_GRIDS ? 2 : 1);
    version_data->piece_sizes.terrain_grid = 52488 * (version > SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION ? 2 : 1);
    version_data->piece_sizes.rubble_grid = 104976;
    version_data->piece_sizes.figures = 128000 * multiplier;
    version_data->piece_sizes.route_figures = 1200 * multiplier;
    version_data->piece_sizes.route_paths = 300000 * multiplier;
    version_data->piece_sizes.formations = 6400 * multiplier;
    version_data->piece_sizes.buildings = 256000 * multiplier;
    version_data->piece_sizes.building_list_burning = 1000 * multiplier;
    version_data->piece_sizes.building_list_small = 1000 * multiplier;
    version_data->piece_sizes.building_list_large = 4000 * multiplier;
    version_data->piece_sizes.building_storages = 6400 * multiplier;
    version_data->piece_sizes.monument_deliveries = version > SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION ? 0 : 3200;
    version_data->piece_sizes.enemy_armies = version > SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG ? (125 * sizeof(int) * 9) : 900;
    version_data->piece_sizes.graph_order = version > SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA ? 4 : 8;
    // Due to an oversight, we only reduced the city_data buffer size when we added dynamic resources
    if (version <= SAVE_GAME_LAST_STATIC_RESOURCES) {
        version_data->piece_sizes.city_data = 36136;
    } else {
        version_data->piece_sizes.city_data = 11885;
        version_data->piece_sizes.city_data -= sizeof(int32_t) * 6 * 2;
        version_data->piece_sizes.city_data += total_new_resources * 18;
        version_data->piece_sizes.city_data += total_new_food * 4;
    }
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        // Bug - this should have acconted the new resource types, but since it hasn't before,
        // let's keep it to prevent crashes on opening. This is outdated anyway and was only available for unstable builds.
        version_data->piece_sizes.empire_cities = 2706;
    } else {
        version_data->piece_sizes.empire_cities = 0;
    }
    version_data->piece_sizes.trade_prices = 8 * resources;
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        version_data->piece_sizes.trade_route_limit = 20 * 4 * resources;
        version_data->piece_sizes.trade_route_traded = 20 * 4 * resources;
    } else {
        version_data->piece_sizes.trade_route_limit = 0;
        version_data->piece_sizes.trade_route_traded = 0;
    }
    version_data->piece_sizes.figure_traders = 1604 + 100 * 2 * resources;

    if (version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        version_data->piece_sizes.invasion_warnings = 0;
    } else {
        version_data->piece_sizes.invasion_warnings = 3232;
    }

    version_data->building_counts.culture1 = 132 * count_multiplier;
    version_data->building_counts.culture2 = 32 * count_multiplier;
    version_data->building_counts.culture3 = 40 * count_multiplier;
    version_data->building_counts.military = 16 * count_multiplier;
    version_data->building_counts.industry = 128 * count_multiplier;
    version_data->building_counts.support = 24 * count_multiplier;

    version_data->features.image_grid = version <= SAVE_GAME_LAST_STORED_IMAGE_IDS;
    version_data->features.monument_deliveries = version > SAVE_GAME_LAST_NO_DELIVERIES_VERSION;
    version_data->features.barracks_tower_sentry_request = version <= SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST;

    version_data->piece_sizes.scenario = scenario_size(version);
    if (version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        version_data->features.custom_empires = 0;
    } else {
        version_data->features.custom_empires = 1;
    }
    version_data->features.requests = version > SAVE_GAME_LAST_NO_EXTENDED_REQUESTS;

    if (version > SAVE_GAME_LAST_NO_EVENTS) {
        version_data->features.scenario_events = 1;
        version_data->features.scenario_conditions = 1;
        version_data->features.scenario_actions = 1;
        if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
            version_data->features.scenario_formulas = 1;
        } else {
            version_data->features.scenario_formulas = 0;
        }
    } else {
        version_data->features.scenario_events = 0;
        version_data->features.scenario_conditions = 0;
        version_data->features.scenario_actions = 0;
        version_data->features.scenario_formulas = 0;
    }

    if (version > SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        version_data->features.custom_messages_and_media = 1;
    } else {
        version_data->features.custom_messages_and_media = 0;
    }

    version_data->features.custom_empire_map_image = version > SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE;
    version_data->features.scenario_version = version > SAVE_GAME_LAST_NO_SCENARIO_VERSION;
    version_data->features.city_faction_info = version <= SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA;
    version_data->features.resource_version = version > SAVE_GAME_LAST_STATIC_RESOURCES;
    version_data->features.static_building_counts = version <= SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.visited_buildings = version > SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.custom_campaigns = version > SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS;
    version_data->features.dynamic_scenario_objects = version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA;
    version_data->features.custom_model_data = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.rubble_grid = version > SAVE_GAME_LAST_U16_GRIDS;
    version_data->features.custom_production_rates = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
}
template <typename Emit> static void visit(int version, int resources, int foods, Emit emit)
{


    savegame_version_data version_data{};
    get_version_data(&version_data, version, resources, foods);


    emit("scenario_campaign_mission", 4, 0);
    emit("file_version", 4, 0);
    if (version_data.features.resource_version) {
        emit("resource_version", 4, 0);
    }
    if (version_data.features.scenario_version) {
        emit("scenario_version", 4, 0);
    }
    if (version_data.features.image_grid) {
        emit("image_grid", version_data.piece_sizes.image_grid, 1);
    }
    emit("edge_grid", 26244, 1);
    emit("building_grid", version_data.piece_sizes.building_grid, 1);
    emit("terrain_grid", version_data.piece_sizes.terrain_grid, 1);
    emit("aqueduct_grid", 26244, 1);
    emit("figure_grid", 52488, 1);
    if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        emit("bitfields_grid", 52488, 1);
    } else {
        emit("bitfields_grid", 26244, 1);
    }
    emit("sprite_grid", 26244, 1);
    emit("random_grid", 26244, 0);
    emit("desirability_grid", 26244, 1);
    emit("elevation_grid", 26244, 1);
    emit("building_damage_grid", 26244, 1);
    emit("aqueduct_backup_grid", 26244, 1);
    emit("sprite_backup_grid", 26244, 1);
    emit("figures", version_data.piece_sizes.figures, 1);
    emit("route_figures", version_data.piece_sizes.route_figures, 1);
    emit("route_paths", version_data.piece_sizes.route_paths, 1);
    emit("formations", version_data.piece_sizes.formations, 1);
    emit("formation_totals", 12, 0);
    emit("city_data", version_data.piece_sizes.city_data, 1);
    if (version_data.features.city_faction_info) {
        emit("city_faction_unknown", 2, 0);
    }
    emit("player_name", 64, 0);
    if (version_data.features.city_faction_info) {
        emit("city_faction", 4, 0);
    }
    emit("buildings", version_data.piece_sizes.buildings, 1);
    emit("city_view_orientation", 4, 0);
    emit("game_time", 20, 0);
    emit("building_extra_highest_id_ever", 8, 0);
    emit("random_iv", 8, 0);
    emit("city_view_camera", 8, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture1", version_data.building_counts.culture1, 0);
    }
    emit("city_graph_order", version_data.piece_sizes.graph_order, 0);
    emit("emperor_change_time", 8, 0);
    emit("empire", 12, 0);
    if (version_data.features.custom_empire_map_image) {
        emit("empire_map", 0, 0);
    }
    emit("empire_cities", version_data.piece_sizes.empire_cities, 1);
    if (version_data.features.static_building_counts) {
        emit("building_count_industry", version_data.building_counts.industry, 0);
    }
    emit("trade_prices", version_data.piece_sizes.trade_prices, 0);
    emit("figure_names", 84, 0);
    emit("culture_coverage", 60, 0);
    emit("scenario", version_data.piece_sizes.scenario, 0);
    if (version_data.features.requests) {
        emit("requests", 0, 0);
    }
    if (version_data.features.dynamic_scenario_objects) {
        emit("invasions", 0, 1);
        emit("demand_changes", 0, 1);
        emit("price_changes", 0, 1);
        emit("allowed_buildings", 0, 1);
        emit("custom_variables", 0, 1);
    }
    if (version_data.features.scenario_events) {
        emit("scenario_events", 0, 0);
    }
    if (version_data.features.scenario_formulas) {
        emit("scenario_formulas", 0, 0);
    }
    if (version_data.features.scenario_conditions) {
        emit("scenario_conditions", 0, 0);
    }
    if (version_data.features.scenario_actions) {
        emit("scenario_actions", 0, 0);
    }
    if (version_data.features.custom_messages_and_media) {
        emit("custom_messages", 0, 0);
        emit("custom_media", 0, 0);
        emit("message_media_text_blob", 0, 0);
        emit("message_media_metadata", 0, 0);
    }
    if (version_data.features.custom_model_data) {
        emit("building_model_data", 0, 0);
    }
    emit("max_game_year", 4, 0);
    emit("earthquake", 60, 0);
    emit("emperor_change_state", 4, 0);
    emit("messages", 16000, 1);
    emit("message_extra", 12, 0);
    emit("population_messages", 10, 0);
    emit("message_counts", 80, 0);
    emit("message_delays", 80, 0);
    emit("building_list_burning_totals", version_data.piece_sizes.burning_totals, 0);
    emit("figure_sequence", 4, 0);
    emit("scenario_settings", 12, 0);
    emit("invasion_warnings", version_data.piece_sizes.invasion_warnings, 1);
    emit("scenario_is_custom", 4, 0);
    emit("city_sounds", 8960, 0);
    emit("building_extra_highest_id", 4, 0);
    emit("figure_traders", version_data.piece_sizes.figure_traders, 0);
    emit("building_list_burning", version_data.piece_sizes.building_list_burning, 1);
    emit("building_list_small", version_data.piece_sizes.building_list_small, 1);
    emit("building_list_large", version_data.piece_sizes.building_list_large, 1);
    emit("tutorial_part1", 32, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_military", version_data.building_counts.military, 0);
    }
    emit("enemy_army_totals", 20, 0);
    emit("building_storages", version_data.piece_sizes.building_storages, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture2", version_data.building_counts.culture2, 0);
        emit("building_count_support", version_data.building_counts.support, 0);
    }
    emit("tutorial_part2", 4, 0);
    emit("gladiator_revolt", 16, 0);
    if (version > SAVE_GAME_LAST_NO_EMPIRE_EDITOR) {
        emit("trade_routes", 0, 1);
    } else {
        emit("trade_route_limit", version_data.piece_sizes.trade_route_limit, 1);
        emit("trade_route_traded", version_data.piece_sizes.trade_route_traded, 1);
    }
    if (version_data.features.barracks_tower_sentry_request) {
        emit("building_barracks_tower_sentry", 4, 0);
    }
    emit("building_extra_sequence", 4, 0);
    emit("routing_counters", 16, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture3", version_data.building_counts.culture3, 0);
    }
    emit("enemy_armies", version_data.piece_sizes.enemy_armies, 0);
    emit("city_entry_exit_xy", 16, 0);
    emit("last_invasion_id", 2, 0);
    emit("building_extra_corrupt_houses", 8, 0);
    emit("scenario_name", 65, 0);
    emit("bookmarks", 32, 0);
    emit("tutorial_part3", 4, 0);
    emit("city_entry_exit_grid_offset", 8, 0);
    if (version_data.features.custom_campaigns) {
        emit("campaign_name", 0, 0);
    }
    emit("end_marker", 284, 0); // 71x 4-bytes emptiness
    if (version_data.features.monument_deliveries) {
        emit("deliveries", version_data.piece_sizes.monument_deliveries, 0);
    }
    if (version_data.features.custom_empires) {
        emit("custom_empire", 0, 1);
    }
    if (version_data.features.visited_buildings) {
        emit("visited_buildings", 0, 1);
    }
    if (version_data.features.rubble_grid) {
        emit("rubble_grid", 104976, 1);
    }
    if (version_data.features.custom_production_rates) {
        emit("production_rates", 0, 1);
    }
}
}


// Producer ecf4278d17edb710cdf268a83df1ed79f07f7953; layout 0a26ef377f60d3e0202d78b9d3c25c857b71d6b5cfe05f3cb54872a64a32390e
namespace layout_1 {
enum {

    SAVE_GAME_CURRENT_VERSION = 0xb7,

    SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION = 0x66,
    SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION = 0x76,
    SAVE_GAME_LAST_NO_DELIVERIES_VERSION = 0x77,
    SAVE_GAME_LAST_STATIC_VERSION = 0x78,
    SAVE_GAME_LAST_JOINED_IMPORT_EXPORT_VERSION = 0x79,
    SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION = 0x80,
    SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION = 0x81,
    SAVE_GAME_LAST_STORED_IMAGE_IDS = 0x83,
    // SAVE_GAME_INCREASE_GRANARY_CAPACITY shall be updated if we decide to change granary capacity again.
    SAVE_GAME_INCREASE_GRANARY_CAPACITY = 0x85,
    SAVE_GAME_ROADBLOCK_DATA_MOVED_FROM_SUBTYPE = 0x86,
    SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION = 0x86,
    SAVE_GAME_LAST_CARAVANSERAI_WRONG_OFFSET = 0x87,
    SAVE_GAME_LAST_ZIP_COMPRESSION = 0x88,
    SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG = 0x89,
    SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST = 0x8a,
    // SAVE_GAME_LAST_WITHOUT_HIGHWAYS = 0x8b, no actual changes to how games are saved. Crudelios just wants this here
    SAVE_GAME_LAST_UNVERSIONED_SCENARIOS = 0x8c,
    SAVE_GAME_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 0x8d,
    // the difference between this version and UNVERSIONED_SCENARIOS above is this one actually saves the scenario version
    // in the data, whereas the previous one did a lookup based on the save version
    SAVE_GAME_LAST_NO_SCENARIO_VERSION = 0x8e,
    SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA = 0x8f,
    SAVE_GAME_LAST_STATIC_RESOURCES = 0x90,
    SAVE_GAME_LAST_GLOBAL_BUILDING_INFO = 0x91,
    SAVE_GAME_LAST_NO_GOLD_AND_MINTING = 0x92,
    SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS = 0x93,
    SAVE_GAME_LAST_NO_EXTENDED_REQUESTS = 0x94,
    SAVE_GAME_LAST_NO_EVENTS = 0x95,
    SAVE_GAME_LAST_NO_CUSTOM_MESSAGES = 0x96,
    SAVE_GAME_LAST_NO_CART_DEPOT = 0x97,
    SAVE_GAME_LAST_NO_NEW_MONUMENT_RESOURCES = 0x98,
    SAVE_GAME_LAST_MONUMENT_TYPE_DATA = 0x99,
    SAVE_GAME_LAST_NO_CUSTOM_VARIABLES = 0x9a,
    SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET = 0x9b,
    SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 0x9c,
    SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS = 0x9d,
    SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA = 0x9e,
    SAVE_GAME_LAST_NO_LATRINES = 0x9f,
    SAVE_GAME_LAST_SPRITE_BRIDGES = 0xa0,
    SAVE_GAME_LAST_SPRITE_BRIDGES_MIGRATION_FIX = 0xa1,
    SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS = 0xa2,
    SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS = 0xa3,
    SAVE_GAME_LAST_STORAGE_STATE_AND_QUANTITY_TOGETHER = 0xa4,
    SAVE_GAME_LAST_10_LEGIONS_MAX = 0xa5,
    SAVE_GAME_LAST_GRANARY_WAREHOUSE_NON_ROADBLOCKS = 0xa6,
    SAVE_GAME_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 0xa7,
    SAVE_GAME_LAST_U16_GRIDS = 0xa8,
    SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA = 0xa9,
    SAVE_GAME_LAST_STATIC_PATHS_AND_ROUTES = 0xaa,
    SAVE_GAME_LAST_NO_EMPIRE_EDITOR = 0xab,
    SAVE_GAME_LAST_LIMITED_ROUTE_COST = 0xac,
    SAVE_GAME_LAST_NO_WATER_IN_ADVISORS = 0xad,
    SAVE_GAME_LAST_NO_SHARED_BUILDINGS = 0xae,
    SAVE_GAME_LAST_NO_PLEBIANS = 0xaf,
    SAVE_GAME_LAST_NO_DOG = 0xb0,
    SAVE_GAME_LAST_ALTAR_ROTATION_FIX = 0xb1,
    SAVE_GAME_LAST_NO_HIGHWAY_STATION = 0xb2,
    SAVE_GAME_LAST_NO_TRIUMPHAL_ARCH_MONUMENT = 0xb3,
    SAVE_GAME_LAST_NO_FORT_ORIENTATION = 0xb4,
    SAVE_GAME_LAST_NO_LEDGER = 0xb5, // only existed in test versions, never on master. 
    SAVE_GAME_LAST_NO_FINANCE_OVERVIEW_HISTORY = 0xb6,
};
enum {
    SCENARIO_CURRENT_VERSION = 22,

    SCENARIO_VERSION_NONE = 0,
    SCENARIO_LAST_UNVERSIONED = 1,
    SCENARIO_LAST_NO_WAGE_LIMITS = 2,
    SCENARIO_LAST_EMPIRE_OBJECT_BUFFERS = 3,
    SCENARIO_LAST_EMPIRE_RESOURCES_U8 = 4,
    SCENARIO_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 5,
    SCENARIO_LAST_NO_SAVE_VERSION_WRITE = 6,
    SCENARIO_LAST_NO_STATIC_RESOURCES = 7,
    SCENARIO_LAST_NO_DYNAMIC_OBJECTS = 8,
    SCENARIO_LAST_NO_EXTENDED_REQUESTS = 9,
    SCENARIO_LAST_NO_EVENTS = 10,
    SCENARIO_LAST_NO_CUSTOM_MESSAGES = 11,
    SCENARIO_LAST_NO_CUSTOM_VARIABLES = 12,
    SCENARIO_LAST_WRONG_END_OFFSET = 13,
    SCENARIO_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 14,
    SCENARIO_LAST_STATIC_ORIGINAL_DATA = 15,
    SCENARIO_LAST_NO_ALT_NATIVE_HUTS = 16,
    SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS = 17,
    SCENARIO_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 18,
    SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA = 19,
    SCENARIO_LAST_NO_EMPIRE_EDITOR = 20,
    SCENARIO_LAST_LIMITED_ROUTE_COST = 21
};
enum {
    SAVEGAME_STATUS_NEWER_VERSION = -1,
    SAVEGAME_STATUS_INVALID = 0,
    SAVEGAME_STATUS_OK = 1
};
typedef struct {
    struct {
        int burning_totals;
        int image_grid;
        int building_grid;
        int rubble_grid;
        int terrain_grid;
        int figures;
        int route_figures;
        int route_paths;
        int formations;
        int buildings;
        int building_list_burning;
        int building_list_small;
        int building_list_large;
        int building_storages;
        int monument_deliveries;
        int enemy_armies;
        int scenario;
        int graph_order;
        int city_data;
        int empire_cities;
        int trade_prices;
        int trade_route_limit;
        int trade_route_traded;
        int figure_traders;
        int invasion_warnings;
    } piece_sizes;
    struct {
        int culture1;
        int culture2;
        int culture3;
        int military;
        int industry;
        int support;
    } building_counts;
    struct {
        int image_grid;
        int monument_deliveries;
        int barracks_tower_sentry_request;
        int custom_empires;
        int custom_empire_map_image;
        int scenario_version;
        int requests;
        int scenario_events;
        int scenario_conditions;
        int scenario_actions;
        int scenario_formulas;
        int custom_messages_and_media;
        int city_faction_info;
        int resource_version;
        int static_building_counts;
        int visited_buildings;
        int custom_campaigns;
        int dynamic_scenario_objects;
        int custom_model_data;
        int rubble_grid;
        int custom_production_rates;
    } features;
} savegame_version_data;
static int scenario_size(int savegame_version)
{
struct {
    size_t size;
    size_t start_info;
    size_t original_requests_part1;
    size_t original_invasions_part1;
    size_t start_funds_and_enemy_id;
    size_t map_size;
    size_t briefing;
    size_t original_requests_part2;
    size_t image;
    size_t herds;
    size_t original_demand_changes;
    size_t original_price_changes;
    size_t gladiator_revolt;
    size_t emperor_change;
    size_t random_events;
    size_t fishing;
    size_t original_requests_part3;
    size_t original_invasions_part2;
    size_t original_requests_part4;
    size_t rome_wheat;
    size_t allowed_buildings;
    size_t win_criteria;
    size_t map_points;
    size_t invasion_points;
    size_t misc;
    size_t alt_huts;
    size_t native_buildings;
    size_t introduction;
    size_t custom_variables;
    size_t custom_name;
    size_t end;

    // Cache info
    int version;
} buffer_offsets{};
auto calculate_buffer_offsets = [&](int scenario_version) {
    
    size_t next_start_offset = 0;

    if (scenario_version > SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.size = next_start_offset;
        next_start_offset = buffer_offsets.size + sizeof(int32_t);
    }

    buffer_offsets.start_info = next_start_offset;
    next_start_offset = buffer_offsets.start_info + 14;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part1 + 20 * 8;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part1 + 20 * 10;
    }

    buffer_offsets.start_funds_and_enemy_id = next_start_offset;
    next_start_offset = buffer_offsets.start_funds_and_enemy_id + 14;

    buffer_offsets.map_size = next_start_offset;
    next_start_offset = buffer_offsets.map_size + 16;

    buffer_offsets.briefing = next_start_offset;
    next_start_offset = buffer_offsets.briefing + 64 + 522;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part2 + 20;
    }

    buffer_offsets.image = next_start_offset;
    next_start_offset = buffer_offsets.image + 6;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 2;// favour reset monthly pattern is +2 bytes
    }
    buffer_offsets.herds = next_start_offset;
    next_start_offset = buffer_offsets.herds + 4 * 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_demand_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_demand_changes + 20 * 9;

        buffer_offsets.original_price_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_price_changes + 20 * 6;
    }

    buffer_offsets.gladiator_revolt = next_start_offset;
    next_start_offset = buffer_offsets.gladiator_revolt + 8;

    buffer_offsets.emperor_change = next_start_offset;
    next_start_offset = buffer_offsets.emperor_change + 8;

    buffer_offsets.random_events = next_start_offset;
    next_start_offset = buffer_offsets.random_events + 36;

    buffer_offsets.fishing = next_start_offset;
    next_start_offset = buffer_offsets.fishing + 8 * 4;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part3 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part3 + 20;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part2 + 20 * 1;
    }

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part4 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part4 + 20 * 4;
    }

    buffer_offsets.rome_wheat = next_start_offset;
    next_start_offset = buffer_offsets.rome_wheat + 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.allowed_buildings = next_start_offset;
        next_start_offset = buffer_offsets.allowed_buildings + 50 * 2;
    }

    buffer_offsets.win_criteria = next_start_offset;
    next_start_offset = buffer_offsets.win_criteria + 52;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 1;// earthkek pattern is +1 byte
    }
    buffer_offsets.map_points = next_start_offset;
    next_start_offset = buffer_offsets.map_points + 12;

    buffer_offsets.invasion_points = next_start_offset;
    next_start_offset = buffer_offsets.invasion_points + 8 * 4;

    buffer_offsets.misc = next_start_offset;
    next_start_offset = buffer_offsets.misc + 51;

    if (scenario_version > SCENARIO_LAST_NO_ALT_NATIVE_HUTS) {
        buffer_offsets.alt_huts = next_start_offset;
        next_start_offset = buffer_offsets.alt_huts + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        buffer_offsets.native_buildings = next_start_offset;
        next_start_offset = buffer_offsets.native_buildings + 12;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_MESSAGES) {
        buffer_offsets.introduction = next_start_offset;
        next_start_offset = buffer_offsets.introduction + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_VARIABLES && scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.custom_variables = next_start_offset;
        next_start_offset = buffer_offsets.custom_variables + 100 * 9;
    }

    buffer_offsets.custom_name = next_start_offset;
    next_start_offset = buffer_offsets.custom_name +
        (scenario_version > SCENARIO_LAST_WRONG_END_OFFSET ? 300 : 50);

    buffer_offsets.end = next_start_offset + 1;

    buffer_offsets.version = scenario_version;

};
    if (savegame_version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        return 1720;
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTENDED_REQUESTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTENDED_REQUESTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_MESSAGES);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_VARIABLES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_VARIABLES);
    } else if (savegame_version <= SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET) {
        calculate_buffer_offsets(SCENARIO_LAST_WRONG_END_OFFSET);
    } else if (savegame_version <= SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_STATIC_ORIGINAL_DATA);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_ALT_NATIVE_HUTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA);
    } else {
        calculate_buffer_offsets(SCENARIO_CURRENT_VERSION);
    }
    return (int) buffer_offsets.end;
}
static void get_version_data(savegame_version_data *version_data, int version, int resources, int foods)
{
    int multiplier = 1;
    int count_multiplier = 1;

    int total_new_resources = resources - 16;
    int total_new_food = foods - 7;

    version_data->piece_sizes.burning_totals = 8;
    if (version > SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION) {
        multiplier = 5;
    }
    if (version > SAVE_GAME_LAST_STATIC_VERSION) {
        multiplier = 0;
        version_data->piece_sizes.burning_totals = 4;
    }

    if (version > SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION) {
        count_multiplier = 0;
    }

    version_data->piece_sizes.image_grid = 52488 * (version > SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION ? 2 : 1);
    version_data->piece_sizes.building_grid = 52488 * (version > SAVE_GAME_LAST_U16_GRIDS ? 2 : 1);
    version_data->piece_sizes.terrain_grid = 52488 * (version > SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION ? 2 : 1);
    version_data->piece_sizes.rubble_grid = 104976;
    version_data->piece_sizes.figures = 128000 * multiplier;
    version_data->piece_sizes.route_figures = 1200 * multiplier;
    version_data->piece_sizes.route_paths = 300000 * multiplier;
    version_data->piece_sizes.formations = 6400 * multiplier;
    version_data->piece_sizes.buildings = 256000 * multiplier;
    version_data->piece_sizes.building_list_burning = 1000 * multiplier;
    version_data->piece_sizes.building_list_small = 1000 * multiplier;
    version_data->piece_sizes.building_list_large = 4000 * multiplier;
    version_data->piece_sizes.building_storages = 6400 * multiplier;
    version_data->piece_sizes.monument_deliveries = version > SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION ? 0 : 3200;
    version_data->piece_sizes.enemy_armies = version > SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG ? (125 * sizeof(int) * 9) : 900;
    version_data->piece_sizes.graph_order = version > SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA ? 4 : 8;
    // Due to an oversight, we only reduced the city_data buffer size when we added dynamic resources
    if (version <= SAVE_GAME_LAST_STATIC_RESOURCES) {
        version_data->piece_sizes.city_data = 36136;
    } else {
        version_data->piece_sizes.city_data = 11885;
        version_data->piece_sizes.city_data -= sizeof(int32_t) * 6 * 2;
        version_data->piece_sizes.city_data += total_new_resources * 18;
        version_data->piece_sizes.city_data += total_new_food * 4;
    }
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        // Bug - this should have acconted the new resource types, but since it hasn't before,
        // let's keep it to prevent crashes on opening. This is outdated anyway and was only available for unstable builds.
        version_data->piece_sizes.empire_cities = 2706;
    } else {
        version_data->piece_sizes.empire_cities = 0;
    }
    version_data->piece_sizes.trade_prices = 8 * resources;
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        version_data->piece_sizes.trade_route_limit = 20 * 4 * resources;
        version_data->piece_sizes.trade_route_traded = 20 * 4 * resources;
    } else {
        version_data->piece_sizes.trade_route_limit = 0;
        version_data->piece_sizes.trade_route_traded = 0;
    }
    version_data->piece_sizes.figure_traders = 1604 + 100 * 2 * resources;

    if (version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        version_data->piece_sizes.invasion_warnings = 0;
    } else {
        version_data->piece_sizes.invasion_warnings = 3232;
    }

    version_data->building_counts.culture1 = 132 * count_multiplier;
    version_data->building_counts.culture2 = 32 * count_multiplier;
    version_data->building_counts.culture3 = 40 * count_multiplier;
    version_data->building_counts.military = 16 * count_multiplier;
    version_data->building_counts.industry = 128 * count_multiplier;
    version_data->building_counts.support = 24 * count_multiplier;

    version_data->features.image_grid = version <= SAVE_GAME_LAST_STORED_IMAGE_IDS;
    version_data->features.monument_deliveries = version > SAVE_GAME_LAST_NO_DELIVERIES_VERSION;
    version_data->features.barracks_tower_sentry_request = version <= SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST;

    version_data->piece_sizes.scenario = scenario_size(version);
    if (version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        version_data->features.custom_empires = 0;
    } else {
        version_data->features.custom_empires = 1;
    }
    version_data->features.requests = version > SAVE_GAME_LAST_NO_EXTENDED_REQUESTS;

    if (version > SAVE_GAME_LAST_NO_EVENTS) {
        version_data->features.scenario_events = 1;
        version_data->features.scenario_conditions = 1;
        version_data->features.scenario_actions = 1;
        if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
            version_data->features.scenario_formulas = 1;
        } else {
            version_data->features.scenario_formulas = 0;
        }
    } else {
        version_data->features.scenario_events = 0;
        version_data->features.scenario_conditions = 0;
        version_data->features.scenario_actions = 0;
        version_data->features.scenario_formulas = 0;
    }

    if (version > SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        version_data->features.custom_messages_and_media = 1;
    } else {
        version_data->features.custom_messages_and_media = 0;
    }

    version_data->features.custom_empire_map_image = version > SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE;
    version_data->features.scenario_version = version > SAVE_GAME_LAST_NO_SCENARIO_VERSION;
    version_data->features.city_faction_info = version <= SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA;
    version_data->features.resource_version = version > SAVE_GAME_LAST_STATIC_RESOURCES;
    version_data->features.static_building_counts = version <= SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.visited_buildings = version > SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.custom_campaigns = version > SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS;
    version_data->features.dynamic_scenario_objects = version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA;
    version_data->features.custom_model_data = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.rubble_grid = version > SAVE_GAME_LAST_U16_GRIDS;
    version_data->features.custom_production_rates = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
}
template <typename Emit> static void visit(int version, int resources, int foods, Emit emit)
{


    savegame_version_data version_data{};
    get_version_data(&version_data, version, resources, foods);


    emit("scenario_campaign_mission", 4, 0);
    emit("file_version", 4, 0);
    if (version_data.features.resource_version) {
        emit("resource_version", 4, 0);
    }
    if (version_data.features.scenario_version) {
        emit("scenario_version", 4, 0);
    }
    if (version_data.features.image_grid) {
        emit("image_grid", version_data.piece_sizes.image_grid, 1);
    }
    emit("edge_grid", 26244, 1);
    emit("building_grid", version_data.piece_sizes.building_grid, 1);
    emit("terrain_grid", version_data.piece_sizes.terrain_grid, 1);
    emit("aqueduct_grid", 26244, 1);
    emit("figure_grid", 52488, 1);
    if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        emit("bitfields_grid", 52488, 1);
    } else {
        emit("bitfields_grid", 26244, 1);
    }
    emit("sprite_grid", 26244, 1);
    emit("random_grid", 26244, 0);
    emit("desirability_grid", 26244, 1);
    emit("elevation_grid", 26244, 1);
    emit("building_damage_grid", 26244, 1);
    emit("aqueduct_backup_grid", 26244, 1);
    emit("sprite_backup_grid", 26244, 1);
    emit("figures", version_data.piece_sizes.figures, 1);
    emit("route_figures", version_data.piece_sizes.route_figures, 1);
    emit("route_paths", version_data.piece_sizes.route_paths, 1);
    emit("formations", version_data.piece_sizes.formations, 1);
    emit("formation_totals", 12, 0);
    emit("city_data", version_data.piece_sizes.city_data, 1);
    if (version_data.features.city_faction_info) {
        emit("city_faction_unknown", 2, 0);
    }
    emit("player_name", 64, 0);
    if (version_data.features.city_faction_info) {
        emit("city_faction", 4, 0);
    }
    emit("buildings", version_data.piece_sizes.buildings, 1);
    emit("city_view_orientation", 4, 0);
    emit("game_time", 20, 0);
    emit("building_extra_highest_id_ever", 8, 0);
    emit("random_iv", 8, 0);
    emit("city_view_camera", 8, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture1", version_data.building_counts.culture1, 0);
    }
    emit("city_graph_order", version_data.piece_sizes.graph_order, 0);
    emit("emperor_change_time", 8, 0);
    emit("empire", 12, 0);
    if (version_data.features.custom_empire_map_image) {
        emit("empire_map", 0, 0);
    }
    emit("empire_cities", version_data.piece_sizes.empire_cities, 1);
    if (version_data.features.static_building_counts) {
        emit("building_count_industry", version_data.building_counts.industry, 0);
    }
    emit("trade_prices", version_data.piece_sizes.trade_prices, 0);
    emit("figure_names", 84, 0);
    emit("culture_coverage", 60, 0);
    emit("scenario", version_data.piece_sizes.scenario, 0);
    if (version_data.features.requests) {
        emit("requests", 0, 0);
    }
    if (version_data.features.dynamic_scenario_objects) {
        emit("invasions", 0, 1);
        emit("demand_changes", 0, 1);
        emit("price_changes", 0, 1);
        emit("allowed_buildings", 0, 1);
        emit("custom_variables", 0, 1);
    }
    if (version_data.features.scenario_events) {
        emit("scenario_events", 0, 0);
    }
    if (version_data.features.scenario_formulas) {
        emit("scenario_formulas", 0, 0);
    }
    if (version_data.features.scenario_conditions) {
        emit("scenario_conditions", 0, 0);
    }
    if (version_data.features.scenario_actions) {
        emit("scenario_actions", 0, 0);
    }
    if (version_data.features.custom_messages_and_media) {
        emit("custom_messages", 0, 0);
        emit("custom_media", 0, 0);
        emit("message_media_text_blob", 0, 0);
        emit("message_media_metadata", 0, 0);
    }
    if (version_data.features.custom_model_data) {
        emit("building_model_data", 0, 0);
    }
    emit("max_game_year", 4, 0);
    emit("earthquake", 60, 0);
    emit("emperor_change_state", 4, 0);
    emit("messages", 16000, 1);
    emit("message_extra", 12, 0);
    emit("population_messages", 10, 0);
    emit("message_counts", 80, 0);
    emit("message_delays", 80, 0);
    emit("building_list_burning_totals", version_data.piece_sizes.burning_totals, 0);
    emit("figure_sequence", 4, 0);
    emit("scenario_settings", 12, 0);
    emit("invasion_warnings", version_data.piece_sizes.invasion_warnings, 1);
    emit("scenario_is_custom", 4, 0);
    emit("city_sounds", 8960, 0);
    emit("building_extra_highest_id", 4, 0);
    emit("figure_traders", version_data.piece_sizes.figure_traders, 0);
    emit("building_list_burning", version_data.piece_sizes.building_list_burning, 1);
    emit("building_list_small", version_data.piece_sizes.building_list_small, 1);
    emit("building_list_large", version_data.piece_sizes.building_list_large, 1);
    emit("tutorial_part1", 32, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_military", version_data.building_counts.military, 0);
    }
    emit("enemy_army_totals", 20, 0);
    emit("building_storages", version_data.piece_sizes.building_storages, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture2", version_data.building_counts.culture2, 0);
        emit("building_count_support", version_data.building_counts.support, 0);
    }
    emit("tutorial_part2", 4, 0);
    emit("gladiator_revolt", 16, 0);
    if (version > SAVE_GAME_LAST_NO_EMPIRE_EDITOR) {
        emit("trade_routes", 0, 1);
        if (version > SAVE_GAME_LAST_NO_LEDGER) {
            emit("trade_history", 0, 1);
        }
    } else {
        emit("trade_route_limit", version_data.piece_sizes.trade_route_limit, 1);
        emit("trade_route_traded", version_data.piece_sizes.trade_route_traded, 1);
    }
    if (version_data.features.barracks_tower_sentry_request) {
        emit("building_barracks_tower_sentry", 4, 0);
    }
    emit("building_extra_sequence", 4, 0);
    emit("routing_counters", 16, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture3", version_data.building_counts.culture3, 0);
    }
    emit("enemy_armies", version_data.piece_sizes.enemy_armies, 0);
    emit("city_entry_exit_xy", 16, 0);
    emit("last_invasion_id", 2, 0);
    emit("building_extra_corrupt_houses", 8, 0);
    emit("scenario_name", 65, 0);
    emit("bookmarks", 32, 0);
    emit("tutorial_part3", 4, 0);
    emit("city_entry_exit_grid_offset", 8, 0);
    if (version_data.features.custom_campaigns) {
        emit("campaign_name", 0, 0);
    }
    emit("end_marker", 284, 0); // 71x 4-bytes emptiness
    if (version_data.features.monument_deliveries) {
        emit("deliveries", version_data.piece_sizes.monument_deliveries, 0);
    }
    if (version_data.features.custom_empires) {
        emit("custom_empire", 0, 1);
    }
    if (version_data.features.visited_buildings) {
        emit("visited_buildings", 0, 1);
    }
    if (version_data.features.rubble_grid) {
        emit("rubble_grid", 104976, 1);
    }
    if (version_data.features.custom_production_rates) {
        emit("production_rates", 0, 1);
    }
    if (version > SAVE_GAME_LAST_NO_LEDGER) {
        emit("finance_ledger", 0, 0);
    }
}
}


// Producer 17b05668bbea273550b8b4beef9a4e03c9601b58; layout 3df6e3e6d2bea7b9879bd9e3aacc17385a63b018b408a1bea3e28322d867d27f
namespace layout_2 {
enum {

    SAVE_GAME_CURRENT_VERSION = 0xb8,

    SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION = 0x66,
    SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION = 0x76,
    SAVE_GAME_LAST_NO_DELIVERIES_VERSION = 0x77,
    SAVE_GAME_LAST_STATIC_VERSION = 0x78,
    SAVE_GAME_LAST_JOINED_IMPORT_EXPORT_VERSION = 0x79,
    SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION = 0x80,
    SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION = 0x81,
    SAVE_GAME_LAST_STORED_IMAGE_IDS = 0x83,
    // SAVE_GAME_INCREASE_GRANARY_CAPACITY shall be updated if we decide to change granary capacity again.
    SAVE_GAME_INCREASE_GRANARY_CAPACITY = 0x85,
    SAVE_GAME_ROADBLOCK_DATA_MOVED_FROM_SUBTYPE = 0x86,
    SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION = 0x86,
    SAVE_GAME_LAST_CARAVANSERAI_WRONG_OFFSET = 0x87,
    SAVE_GAME_LAST_ZIP_COMPRESSION = 0x88,
    SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG = 0x89,
    SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST = 0x8a,
    // SAVE_GAME_LAST_WITHOUT_HIGHWAYS = 0x8b, no actual changes to how games are saved. Crudelios just wants this here
    SAVE_GAME_LAST_UNVERSIONED_SCENARIOS = 0x8c,
    SAVE_GAME_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 0x8d,
    // the difference between this version and UNVERSIONED_SCENARIOS above is this one actually saves the scenario version
    // in the data, whereas the previous one did a lookup based on the save version
    SAVE_GAME_LAST_NO_SCENARIO_VERSION = 0x8e,
    SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA = 0x8f,
    SAVE_GAME_LAST_STATIC_RESOURCES = 0x90,
    SAVE_GAME_LAST_GLOBAL_BUILDING_INFO = 0x91,
    SAVE_GAME_LAST_NO_GOLD_AND_MINTING = 0x92,
    SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS = 0x93,
    SAVE_GAME_LAST_NO_EXTENDED_REQUESTS = 0x94,
    SAVE_GAME_LAST_NO_EVENTS = 0x95,
    SAVE_GAME_LAST_NO_CUSTOM_MESSAGES = 0x96,
    SAVE_GAME_LAST_NO_CART_DEPOT = 0x97,
    SAVE_GAME_LAST_NO_NEW_MONUMENT_RESOURCES = 0x98,
    SAVE_GAME_LAST_MONUMENT_TYPE_DATA = 0x99,
    SAVE_GAME_LAST_NO_CUSTOM_VARIABLES = 0x9a,
    SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET = 0x9b,
    SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 0x9c,
    SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS = 0x9d,
    SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA = 0x9e,
    SAVE_GAME_LAST_NO_LATRINES = 0x9f,
    SAVE_GAME_LAST_SPRITE_BRIDGES = 0xa0,
    SAVE_GAME_LAST_SPRITE_BRIDGES_MIGRATION_FIX = 0xa1,
    SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS = 0xa2,
    SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS = 0xa3,
    SAVE_GAME_LAST_STORAGE_STATE_AND_QUANTITY_TOGETHER = 0xa4,
    SAVE_GAME_LAST_10_LEGIONS_MAX = 0xa5,
    SAVE_GAME_LAST_GRANARY_WAREHOUSE_NON_ROADBLOCKS = 0xa6,
    SAVE_GAME_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 0xa7,
    SAVE_GAME_LAST_U16_GRIDS = 0xa8,
    SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA = 0xa9,
    SAVE_GAME_LAST_STATIC_PATHS_AND_ROUTES = 0xaa,
    SAVE_GAME_LAST_NO_EMPIRE_EDITOR = 0xab,
    SAVE_GAME_LAST_LIMITED_ROUTE_COST = 0xac,
    SAVE_GAME_LAST_NO_WATER_IN_ADVISORS = 0xad,
    SAVE_GAME_LAST_NO_SHARED_BUILDINGS = 0xae,
    SAVE_GAME_LAST_NO_PLEBIANS = 0xaf,
    SAVE_GAME_LAST_NO_DOG = 0xb0,
    SAVE_GAME_LAST_ALTAR_ROTATION_FIX = 0xb1,
    SAVE_GAME_LAST_NO_HIGHWAY_STATION = 0xb2,
    SAVE_GAME_LAST_NO_TRIUMPHAL_ARCH_MONUMENT = 0xb3,
    SAVE_GAME_LAST_NO_FORT_ORIENTATION = 0xb4,
    SAVE_GAME_LAST_NO_LEDGER = 0xb5, // only existed in test versions, never on master.
    SAVE_GAME_LAST_NO_FINANCE_OVERVIEW_HISTORY = 0xb6,
    SAVE_GAME_LAST_NO_HOUSE_MODELS = 0xb7,
};
enum {
    SCENARIO_CURRENT_VERSION = 23,

    SCENARIO_VERSION_NONE = 0,
    SCENARIO_LAST_UNVERSIONED = 1,
    SCENARIO_LAST_NO_WAGE_LIMITS = 2,
    SCENARIO_LAST_EMPIRE_OBJECT_BUFFERS = 3,
    SCENARIO_LAST_EMPIRE_RESOURCES_U8 = 4,
    SCENARIO_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 5,
    SCENARIO_LAST_NO_SAVE_VERSION_WRITE = 6,
    SCENARIO_LAST_NO_STATIC_RESOURCES = 7,
    SCENARIO_LAST_NO_DYNAMIC_OBJECTS = 8,
    SCENARIO_LAST_NO_EXTENDED_REQUESTS = 9,
    SCENARIO_LAST_NO_EVENTS = 10,
    SCENARIO_LAST_NO_CUSTOM_MESSAGES = 11,
    SCENARIO_LAST_NO_CUSTOM_VARIABLES = 12,
    SCENARIO_LAST_WRONG_END_OFFSET = 13,
    SCENARIO_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 14,
    SCENARIO_LAST_STATIC_ORIGINAL_DATA = 15,
    SCENARIO_LAST_NO_ALT_NATIVE_HUTS = 16,
    SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS = 17,
    SCENARIO_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 18,
    SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA = 19,
    SCENARIO_LAST_NO_EMPIRE_EDITOR = 20,
    SCENARIO_LAST_LIMITED_ROUTE_COST = 21,
    SCENARIO_LAST_NO_HOUSE_MODELS = 22
};
enum {
    SAVEGAME_STATUS_NEWER_VERSION = -1,
    SAVEGAME_STATUS_INVALID = 0,
    SAVEGAME_STATUS_OK = 1
};
typedef struct {
    struct {
        int burning_totals;
        int image_grid;
        int building_grid;
        int rubble_grid;
        int terrain_grid;
        int figures;
        int route_figures;
        int route_paths;
        int formations;
        int buildings;
        int building_list_burning;
        int building_list_small;
        int building_list_large;
        int building_storages;
        int monument_deliveries;
        int enemy_armies;
        int scenario;
        int graph_order;
        int city_data;
        int empire_cities;
        int trade_prices;
        int trade_route_limit;
        int trade_route_traded;
        int figure_traders;
        int invasion_warnings;
    } piece_sizes;
    struct {
        int culture1;
        int culture2;
        int culture3;
        int military;
        int industry;
        int support;
    } building_counts;
    struct {
        int image_grid;
        int monument_deliveries;
        int barracks_tower_sentry_request;
        int custom_empires;
        int custom_empire_map_image;
        int scenario_version;
        int requests;
        int scenario_events;
        int scenario_conditions;
        int scenario_actions;
        int scenario_formulas;
        int scenario_texts;
        int custom_messages_and_media;
        int city_faction_info;
        int resource_version;
        int static_building_counts;
        int visited_buildings;
        int custom_campaigns;
        int dynamic_scenario_objects;
        int custom_model_data;
        int rubble_grid;
        int custom_production_rates;
        int monument_stages;
    } features;
} savegame_version_data;
static int scenario_size(int savegame_version)
{
struct {
    size_t size;
    size_t start_info;
    size_t original_requests_part1;
    size_t original_invasions_part1;
    size_t start_funds_and_enemy_id;
    size_t map_size;
    size_t briefing;
    size_t original_requests_part2;
    size_t image;
    size_t herds;
    size_t original_demand_changes;
    size_t original_price_changes;
    size_t gladiator_revolt;
    size_t emperor_change;
    size_t random_events;
    size_t fishing;
    size_t original_requests_part3;
    size_t original_invasions_part2;
    size_t original_requests_part4;
    size_t rome_wheat;
    size_t allowed_buildings;
    size_t win_criteria;
    size_t map_points;
    size_t invasion_points;
    size_t misc;
    size_t alt_huts;
    size_t native_buildings;
    size_t introduction;
    size_t custom_variables;
    size_t custom_name;
    size_t end;

    // Cache info
    int version;
} buffer_offsets{};
auto calculate_buffer_offsets = [&](int scenario_version) {
    
    size_t next_start_offset = 0;

    if (scenario_version > SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.size = next_start_offset;
        next_start_offset = buffer_offsets.size + sizeof(int32_t);
    }

    buffer_offsets.start_info = next_start_offset;
    next_start_offset = buffer_offsets.start_info + 14;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part1 + 20 * 8;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part1 + 20 * 10;
    }

    buffer_offsets.start_funds_and_enemy_id = next_start_offset;
    next_start_offset = buffer_offsets.start_funds_and_enemy_id + 14;

    buffer_offsets.map_size = next_start_offset;
    next_start_offset = buffer_offsets.map_size + 16;

    buffer_offsets.briefing = next_start_offset;
    next_start_offset = buffer_offsets.briefing + 64 + 522;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part2 + 20;
    }

    buffer_offsets.image = next_start_offset;
    next_start_offset = buffer_offsets.image + 6;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 2;// favour reset monthly pattern is +2 bytes
    }
    buffer_offsets.herds = next_start_offset;
    next_start_offset = buffer_offsets.herds + 4 * 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_demand_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_demand_changes + 20 * 9;

        buffer_offsets.original_price_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_price_changes + 20 * 6;
    }

    buffer_offsets.gladiator_revolt = next_start_offset;
    next_start_offset = buffer_offsets.gladiator_revolt + 8;

    buffer_offsets.emperor_change = next_start_offset;
    next_start_offset = buffer_offsets.emperor_change + 8;

    buffer_offsets.random_events = next_start_offset;
    next_start_offset = buffer_offsets.random_events + 36;

    buffer_offsets.fishing = next_start_offset;
    next_start_offset = buffer_offsets.fishing + 8 * 4;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part3 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part3 + 20;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part2 + 20 * 1;
    }

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part4 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part4 + 20 * 4;
    }

    buffer_offsets.rome_wheat = next_start_offset;
    next_start_offset = buffer_offsets.rome_wheat + 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.allowed_buildings = next_start_offset;
        next_start_offset = buffer_offsets.allowed_buildings + 50 * 2;
    }

    buffer_offsets.win_criteria = next_start_offset;
    next_start_offset = buffer_offsets.win_criteria + 52;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 1;// earthkek pattern is +1 byte
    }
    buffer_offsets.map_points = next_start_offset;
    next_start_offset = buffer_offsets.map_points + 12;

    buffer_offsets.invasion_points = next_start_offset;
    next_start_offset = buffer_offsets.invasion_points + 8 * 4;

    buffer_offsets.misc = next_start_offset;
    next_start_offset = buffer_offsets.misc + 51;

    if (scenario_version > SCENARIO_LAST_NO_ALT_NATIVE_HUTS) {
        buffer_offsets.alt_huts = next_start_offset;
        next_start_offset = buffer_offsets.alt_huts + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        buffer_offsets.native_buildings = next_start_offset;
        next_start_offset = buffer_offsets.native_buildings + 12;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_MESSAGES) {
        buffer_offsets.introduction = next_start_offset;
        next_start_offset = buffer_offsets.introduction + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_VARIABLES && scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.custom_variables = next_start_offset;
        next_start_offset = buffer_offsets.custom_variables + 100 * 9;
    }

    buffer_offsets.custom_name = next_start_offset;
    next_start_offset = buffer_offsets.custom_name +
        (scenario_version > SCENARIO_LAST_WRONG_END_OFFSET ? 300 : 50);

    buffer_offsets.end = next_start_offset + 1;

    buffer_offsets.version = scenario_version;

};
    if (savegame_version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        return 1720;
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTENDED_REQUESTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTENDED_REQUESTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_MESSAGES);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_VARIABLES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_VARIABLES);
    } else if (savegame_version <= SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET) {
        calculate_buffer_offsets(SCENARIO_LAST_WRONG_END_OFFSET);
    } else if (savegame_version <= SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_STATIC_ORIGINAL_DATA);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_ALT_NATIVE_HUTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA);
    } else {
        calculate_buffer_offsets(SCENARIO_CURRENT_VERSION);
    }
    return (int) buffer_offsets.end;
}
static void get_version_data(savegame_version_data *version_data, int version, int resources, int foods)
{
    int multiplier = 1;
    int count_multiplier = 1;

    int total_new_resources = resources - 16;
    int total_new_food = foods - 7;

    version_data->piece_sizes.burning_totals = 8;
    if (version > SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION) {
        multiplier = 5;
    }
    if (version > SAVE_GAME_LAST_STATIC_VERSION) {
        multiplier = 0;
        version_data->piece_sizes.burning_totals = 4;
    }

    if (version > SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION) {
        count_multiplier = 0;
    }

    version_data->piece_sizes.image_grid = 52488 * (version > SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION ? 2 : 1);
    version_data->piece_sizes.building_grid = 52488 * (version > SAVE_GAME_LAST_U16_GRIDS ? 2 : 1);
    version_data->piece_sizes.terrain_grid = 52488 * (version > SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION ? 2 : 1);
    version_data->piece_sizes.rubble_grid = 104976;
    version_data->piece_sizes.figures = 128000 * multiplier;
    version_data->piece_sizes.route_figures = 1200 * multiplier;
    version_data->piece_sizes.route_paths = 300000 * multiplier;
    version_data->piece_sizes.formations = 6400 * multiplier;
    version_data->piece_sizes.buildings = 256000 * multiplier;
    version_data->piece_sizes.building_list_burning = 1000 * multiplier;
    version_data->piece_sizes.building_list_small = 1000 * multiplier;
    version_data->piece_sizes.building_list_large = 4000 * multiplier;
    version_data->piece_sizes.building_storages = 6400 * multiplier;
    version_data->piece_sizes.monument_deliveries = version > SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION ? 0 : 3200;
    version_data->piece_sizes.enemy_armies = version > SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG ? (125 * sizeof(int) * 9) : 900;
    version_data->piece_sizes.graph_order = version > SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA ? 4 : 8;
    // Due to an oversight, we only reduced the city_data buffer size when we added dynamic resources
    if (version <= SAVE_GAME_LAST_STATIC_RESOURCES) {
        version_data->piece_sizes.city_data = 36136;
    } else {
        version_data->piece_sizes.city_data = 11885;
        version_data->piece_sizes.city_data -= sizeof(int32_t) * 6 * 2;
        version_data->piece_sizes.city_data += total_new_resources * 18;
        version_data->piece_sizes.city_data += total_new_food * 4;
    }
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        // Bug - this should have acconted the new resource types, but since it hasn't before,
        // let's keep it to prevent crashes on opening. This is outdated anyway and was only available for unstable builds.
        version_data->piece_sizes.empire_cities = 2706;
    } else {
        version_data->piece_sizes.empire_cities = 0;
    }
    version_data->piece_sizes.trade_prices = 8 * resources;
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        version_data->piece_sizes.trade_route_limit = 20 * 4 * resources;
        version_data->piece_sizes.trade_route_traded = 20 * 4 * resources;
    } else {
        version_data->piece_sizes.trade_route_limit = 0;
        version_data->piece_sizes.trade_route_traded = 0;
    }
    version_data->piece_sizes.figure_traders = 1604 + 100 * 2 * resources;

    if (version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        version_data->piece_sizes.invasion_warnings = 0;
    } else {
        version_data->piece_sizes.invasion_warnings = 3232;
    }

    version_data->building_counts.culture1 = 132 * count_multiplier;
    version_data->building_counts.culture2 = 32 * count_multiplier;
    version_data->building_counts.culture3 = 40 * count_multiplier;
    version_data->building_counts.military = 16 * count_multiplier;
    version_data->building_counts.industry = 128 * count_multiplier;
    version_data->building_counts.support = 24 * count_multiplier;

    version_data->features.image_grid = version <= SAVE_GAME_LAST_STORED_IMAGE_IDS;
    version_data->features.monument_deliveries = version > SAVE_GAME_LAST_NO_DELIVERIES_VERSION;
    version_data->features.barracks_tower_sentry_request = version <= SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST;

    version_data->piece_sizes.scenario = scenario_size(version);
    if (version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        version_data->features.custom_empires = 0;
    } else {
        version_data->features.custom_empires = 1;
    }
    version_data->features.requests = version > SAVE_GAME_LAST_NO_EXTENDED_REQUESTS;

    if (version > SAVE_GAME_LAST_NO_EVENTS) {
        version_data->features.scenario_events = 1;
        version_data->features.scenario_conditions = 1;
        version_data->features.scenario_actions = 1;
        if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
            version_data->features.scenario_formulas = 1;
        } else {
            version_data->features.scenario_formulas = 0;
        }
        if (version > SAVE_GAME_LAST_NO_HOUSE_MODELS) {
            version_data->features.scenario_texts = 1;
        } else {
            version_data->features.scenario_texts = 0;
        }
    } else {
        version_data->features.scenario_events = 0;
        version_data->features.scenario_conditions = 0;
        version_data->features.scenario_actions = 0;
        version_data->features.scenario_formulas = 0;
        version_data->features.scenario_texts = 0;
    }

    if (version > SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        version_data->features.custom_messages_and_media = 1;
    } else {
        version_data->features.custom_messages_and_media = 0;
    }

    version_data->features.custom_empire_map_image = version > SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE;
    version_data->features.scenario_version = version > SAVE_GAME_LAST_NO_SCENARIO_VERSION;
    version_data->features.city_faction_info = version <= SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA;
    version_data->features.resource_version = version > SAVE_GAME_LAST_STATIC_RESOURCES;
    version_data->features.static_building_counts = version <= SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.visited_buildings = version > SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.custom_campaigns = version > SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS;
    version_data->features.dynamic_scenario_objects = version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA;
    version_data->features.custom_model_data = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.rubble_grid = version > SAVE_GAME_LAST_U16_GRIDS;
    version_data->features.custom_production_rates = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.monument_stages = version > SAVE_GAME_LAST_NO_HOUSE_MODELS;
}
template <typename Emit> static void visit(int version, int resources, int foods, Emit emit)
{


    savegame_version_data version_data{};
    get_version_data(&version_data, version, resources, foods);


    emit("scenario_campaign_mission", 4, 0);
    emit("file_version", 4, 0);
    if (version_data.features.resource_version) {
        emit("resource_version", 4, 0);
    }
    if (version_data.features.scenario_version) {
        emit("scenario_version", 4, 0);
    }
    if (version_data.features.image_grid) {
        emit("image_grid", version_data.piece_sizes.image_grid, 1);
    }
    emit("edge_grid", 26244, 1);
    emit("building_grid", version_data.piece_sizes.building_grid, 1);
    emit("terrain_grid", version_data.piece_sizes.terrain_grid, 1);
    emit("aqueduct_grid", 26244, 1);
    emit("figure_grid", 52488, 1);
    if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        emit("bitfields_grid", 52488, 1);
    } else {
        emit("bitfields_grid", 26244, 1);
    }
    emit("sprite_grid", 26244, 1);
    emit("random_grid", 26244, 0);
    emit("desirability_grid", 26244, 1);
    emit("elevation_grid", 26244, 1);
    emit("building_damage_grid", 26244, 1);
    emit("aqueduct_backup_grid", 26244, 1);
    emit("sprite_backup_grid", 26244, 1);
    emit("figures", version_data.piece_sizes.figures, 1);
    emit("route_figures", version_data.piece_sizes.route_figures, 1);
    emit("route_paths", version_data.piece_sizes.route_paths, 1);
    emit("formations", version_data.piece_sizes.formations, 1);
    emit("formation_totals", 12, 0);
    emit("city_data", version_data.piece_sizes.city_data, 1);
    if (version_data.features.city_faction_info) {
        emit("city_faction_unknown", 2, 0);
    }
    emit("player_name", 64, 0);
    if (version_data.features.city_faction_info) {
        emit("city_faction", 4, 0);
    }
    emit("buildings", version_data.piece_sizes.buildings, 1);
    emit("city_view_orientation", 4, 0);
    emit("game_time", 20, 0);
    emit("building_extra_highest_id_ever", 8, 0);
    emit("random_iv", 8, 0);
    emit("city_view_camera", 8, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture1", version_data.building_counts.culture1, 0);
    }
    emit("city_graph_order", version_data.piece_sizes.graph_order, 0);
    emit("emperor_change_time", 8, 0);
    emit("empire", 12, 0);
    if (version_data.features.custom_empire_map_image) {
        emit("empire_map", 0, 0);
    }
    emit("empire_cities", version_data.piece_sizes.empire_cities, 1);
    if (version_data.features.static_building_counts) {
        emit("building_count_industry", version_data.building_counts.industry, 0);
    }
    emit("trade_prices", version_data.piece_sizes.trade_prices, 0);
    emit("figure_names", 84, 0);
    emit("culture_coverage", 60, 0);
    emit("scenario", version_data.piece_sizes.scenario, 0);
    if (version_data.features.requests) {
        emit("requests", 0, 0);
    }
    if (version_data.features.dynamic_scenario_objects) {
        emit("invasions", 0, 1);
        emit("demand_changes", 0, 1);
        emit("price_changes", 0, 1);
        emit("allowed_buildings", 0, 1);
        emit("custom_variables", 0, 1);
    }
    if (version_data.features.scenario_events) {
        emit("scenario_events", 0, 0);
    }
    if (version_data.features.scenario_formulas) {
        emit("scenario_formulas", 0, 0);
    }
    if (version_data.features.scenario_texts) {
        emit("scenario_texts", 0, 0);
    }
    if (version_data.features.scenario_conditions) {
        emit("scenario_conditions", 0, 0);
    }
    if (version_data.features.scenario_actions) {
        emit("scenario_actions", 0, 0);
    }
    if (version_data.features.custom_messages_and_media) {
        emit("custom_messages", 0, 0);
        emit("custom_media", 0, 0);
        emit("message_media_text_blob", 0, 0);
        emit("message_media_metadata", 0, 0);
    }
    if (version_data.features.custom_model_data) {
        emit("model_data", 0, 0);
    }
    emit("max_game_year", 4, 0);
    emit("earthquake", 60, 0);
    emit("emperor_change_state", 4, 0);
    emit("messages", 16000, 1);
    emit("message_extra", 12, 0);
    emit("population_messages", 10, 0);
    emit("message_counts", 80, 0);
    emit("message_delays", 80, 0);
    emit("building_list_burning_totals", version_data.piece_sizes.burning_totals, 0);
    emit("figure_sequence", 4, 0);
    emit("scenario_settings", 12, 0);
    emit("invasion_warnings", version_data.piece_sizes.invasion_warnings, 1);
    emit("scenario_is_custom", 4, 0);
    emit("city_sounds", 8960, 0);
    emit("building_extra_highest_id", 4, 0);
    emit("figure_traders", version_data.piece_sizes.figure_traders, 0);
    emit("building_list_burning", version_data.piece_sizes.building_list_burning, 1);
    emit("building_list_small", version_data.piece_sizes.building_list_small, 1);
    emit("building_list_large", version_data.piece_sizes.building_list_large, 1);
    emit("tutorial_part1", 32, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_military", version_data.building_counts.military, 0);
    }
    emit("enemy_army_totals", 20, 0);
    emit("building_storages", version_data.piece_sizes.building_storages, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture2", version_data.building_counts.culture2, 0);
        emit("building_count_support", version_data.building_counts.support, 0);
    }
    emit("tutorial_part2", 4, 0);
    emit("gladiator_revolt", 16, 0);
    if (version > SAVE_GAME_LAST_NO_EMPIRE_EDITOR) {
        emit("trade_routes", 0, 1);
        if (version > SAVE_GAME_LAST_NO_LEDGER) {
            emit("trade_history", 0, 1);
        }
    } else {
        emit("trade_route_limit", version_data.piece_sizes.trade_route_limit, 1);
        emit("trade_route_traded", version_data.piece_sizes.trade_route_traded, 1);
    }
    if (version_data.features.barracks_tower_sentry_request) {
        emit("building_barracks_tower_sentry", 4, 0);
    }
    emit("building_extra_sequence", 4, 0);
    emit("routing_counters", 16, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture3", version_data.building_counts.culture3, 0);
    }
    emit("enemy_armies", version_data.piece_sizes.enemy_armies, 0);
    emit("city_entry_exit_xy", 16, 0);
    emit("last_invasion_id", 2, 0);
    emit("building_extra_corrupt_houses", 8, 0);
    emit("scenario_name", 65, 0);
    emit("bookmarks", 32, 0);
    emit("tutorial_part3", 4, 0);
    emit("city_entry_exit_grid_offset", 8, 0);
    if (version_data.features.custom_campaigns) {
        emit("campaign_name", 0, 0);
    }
    emit("end_marker", 284, 0); // 71x 4-bytes emptiness
    if (version_data.features.monument_deliveries) {
        emit("deliveries", version_data.piece_sizes.monument_deliveries, 0);
    }
    if (version_data.features.custom_empires) {
        emit("custom_empire", 0, 1);
    }
    if (version_data.features.visited_buildings) {
        emit("visited_buildings", 0, 1);
    }
    if (version_data.features.rubble_grid) {
        emit("rubble_grid", 104976, 1);
    }
    if (version_data.features.custom_production_rates) {
        emit("production_rates", 0, 1);
    }
    if (version_data.features.monument_stages) {
        emit("monument_stages", 0, 1);
    }
    if (version > SAVE_GAME_LAST_NO_LEDGER) {
        emit("finance_ledger", 0, 0);
    }
}
}


// Producer a20aa0dd93a49f7faedc3c0edbc86c71f84ae0f9; layout 064fe95e5518f79602b51212cfee5a3eca0df5fd67190c7a69012c8f29aa0a4a
namespace layout_3 {
enum {

    SAVE_GAME_CURRENT_VERSION = 0xba,

    SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION = 0x66,
    SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION = 0x76,
    SAVE_GAME_LAST_NO_DELIVERIES_VERSION = 0x77,
    SAVE_GAME_LAST_STATIC_VERSION = 0x78,
    SAVE_GAME_LAST_JOINED_IMPORT_EXPORT_VERSION = 0x79,
    SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION = 0x80,
    SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION = 0x81,
    SAVE_GAME_LAST_STORED_IMAGE_IDS = 0x83,
    // SAVE_GAME_INCREASE_GRANARY_CAPACITY shall be updated if we decide to change granary capacity again.
    SAVE_GAME_INCREASE_GRANARY_CAPACITY = 0x85,
    SAVE_GAME_ROADBLOCK_DATA_MOVED_FROM_SUBTYPE = 0x86,
    SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION = 0x86,
    SAVE_GAME_LAST_CARAVANSERAI_WRONG_OFFSET = 0x87,
    SAVE_GAME_LAST_ZIP_COMPRESSION = 0x88,
    SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG = 0x89,
    SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST = 0x8a,
    // SAVE_GAME_LAST_WITHOUT_HIGHWAYS = 0x8b, no actual changes to how games are saved. Crudelios just wants this here
    SAVE_GAME_LAST_UNVERSIONED_SCENARIOS = 0x8c,
    SAVE_GAME_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 0x8d,
    // the difference between this version and UNVERSIONED_SCENARIOS above is this one actually saves the scenario version
    // in the data, whereas the previous one did a lookup based on the save version
    SAVE_GAME_LAST_NO_SCENARIO_VERSION = 0x8e,
    SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA = 0x8f,
    SAVE_GAME_LAST_STATIC_RESOURCES = 0x90,
    SAVE_GAME_LAST_GLOBAL_BUILDING_INFO = 0x91,
    SAVE_GAME_LAST_NO_GOLD_AND_MINTING = 0x92,
    SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS = 0x93,
    SAVE_GAME_LAST_NO_EXTENDED_REQUESTS = 0x94,
    SAVE_GAME_LAST_NO_EVENTS = 0x95,
    SAVE_GAME_LAST_NO_CUSTOM_MESSAGES = 0x96,
    SAVE_GAME_LAST_NO_CART_DEPOT = 0x97,
    SAVE_GAME_LAST_NO_NEW_MONUMENT_RESOURCES = 0x98,
    SAVE_GAME_LAST_MONUMENT_TYPE_DATA = 0x99,
    SAVE_GAME_LAST_NO_CUSTOM_VARIABLES = 0x9a,
    SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET = 0x9b,
    SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 0x9c,
    SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS = 0x9d,
    SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA = 0x9e,
    SAVE_GAME_LAST_NO_LATRINES = 0x9f,
    SAVE_GAME_LAST_SPRITE_BRIDGES = 0xa0,
    SAVE_GAME_LAST_SPRITE_BRIDGES_MIGRATION_FIX = 0xa1,
    SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS = 0xa2,
    SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS = 0xa3,
    SAVE_GAME_LAST_STORAGE_STATE_AND_QUANTITY_TOGETHER = 0xa4,
    SAVE_GAME_LAST_10_LEGIONS_MAX = 0xa5,
    SAVE_GAME_LAST_GRANARY_WAREHOUSE_NON_ROADBLOCKS = 0xa6,
    SAVE_GAME_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 0xa7,
    SAVE_GAME_LAST_U16_GRIDS = 0xa8,
    SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA = 0xa9,
    SAVE_GAME_LAST_STATIC_PATHS_AND_ROUTES = 0xaa,
    SAVE_GAME_LAST_NO_EMPIRE_EDITOR = 0xab,
    SAVE_GAME_LAST_LIMITED_ROUTE_COST = 0xac,
    SAVE_GAME_LAST_NO_WATER_IN_ADVISORS = 0xad,
    SAVE_GAME_LAST_NO_SHARED_BUILDINGS = 0xae,
    SAVE_GAME_LAST_NO_PLEBIANS = 0xaf,
    SAVE_GAME_LAST_NO_DOG = 0xb0,
    SAVE_GAME_LAST_ALTAR_ROTATION_FIX = 0xb1,
    SAVE_GAME_LAST_NO_HIGHWAY_STATION = 0xb2,
    SAVE_GAME_LAST_NO_TRIUMPHAL_ARCH_MONUMENT = 0xb3,
    SAVE_GAME_LAST_NO_FORT_ORIENTATION = 0xb4,
    SAVE_GAME_LAST_NO_LEDGER = 0xb5, // only existed in test versions, never on master.
    SAVE_GAME_LAST_NO_FINANCE_OVERVIEW_HISTORY = 0xb6,
    SAVE_GAME_LAST_NO_HOUSE_MODELS = 0xb7,
    SAVE_GAME_LAST_NO_FIXED_FOURTH_RELIGIONS = 0xb8,
    SAVE_GAME_LAST_NO_FIXED_CITY_DATA_SIZE = 0xb9
};
enum {
    SCENARIO_CURRENT_VERSION = 23,

    SCENARIO_VERSION_NONE = 0,
    SCENARIO_LAST_UNVERSIONED = 1,
    SCENARIO_LAST_NO_WAGE_LIMITS = 2,
    SCENARIO_LAST_EMPIRE_OBJECT_BUFFERS = 3,
    SCENARIO_LAST_EMPIRE_RESOURCES_U8 = 4,
    SCENARIO_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 5,
    SCENARIO_LAST_NO_SAVE_VERSION_WRITE = 6,
    SCENARIO_LAST_NO_STATIC_RESOURCES = 7,
    SCENARIO_LAST_NO_DYNAMIC_OBJECTS = 8,
    SCENARIO_LAST_NO_EXTENDED_REQUESTS = 9,
    SCENARIO_LAST_NO_EVENTS = 10,
    SCENARIO_LAST_NO_CUSTOM_MESSAGES = 11,
    SCENARIO_LAST_NO_CUSTOM_VARIABLES = 12,
    SCENARIO_LAST_WRONG_END_OFFSET = 13,
    SCENARIO_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 14,
    SCENARIO_LAST_STATIC_ORIGINAL_DATA = 15,
    SCENARIO_LAST_NO_ALT_NATIVE_HUTS = 16,
    SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS = 17,
    SCENARIO_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 18,
    SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA = 19,
    SCENARIO_LAST_NO_EMPIRE_EDITOR = 20,
    SCENARIO_LAST_LIMITED_ROUTE_COST = 21,
    SCENARIO_LAST_NO_HOUSE_MODELS = 22
};
enum {
    SAVEGAME_STATUS_NEWER_VERSION = -1,
    SAVEGAME_STATUS_INVALID = 0,
    SAVEGAME_STATUS_OK = 1
};
typedef struct {
    struct {
        int burning_totals;
        int image_grid;
        int building_grid;
        int rubble_grid;
        int terrain_grid;
        int figures;
        int route_figures;
        int route_paths;
        int formations;
        int buildings;
        int building_list_burning;
        int building_list_small;
        int building_list_large;
        int building_storages;
        int monument_deliveries;
        int enemy_armies;
        int scenario;
        int graph_order;
        int city_data;
        int empire_cities;
        int trade_prices;
        int trade_route_limit;
        int trade_route_traded;
        int figure_traders;
        int invasion_warnings;
    } piece_sizes;
    struct {
        int culture1;
        int culture2;
        int culture3;
        int military;
        int industry;
        int support;
    } building_counts;
    struct {
        int image_grid;
        int monument_deliveries;
        int barracks_tower_sentry_request;
        int custom_empires;
        int custom_empire_map_image;
        int scenario_version;
        int requests;
        int scenario_events;
        int scenario_conditions;
        int scenario_actions;
        int scenario_formulas;
        int scenario_texts;
        int custom_messages_and_media;
        int city_faction_info;
        int resource_version;
        int static_building_counts;
        int visited_buildings;
        int custom_campaigns;
        int dynamic_scenario_objects;
        int custom_model_data;
        int rubble_grid;
        int custom_production_rates;
        int monument_stages;
    } features;
} savegame_version_data;
static int scenario_size(int savegame_version)
{
struct {
    size_t size;
    size_t start_info;
    size_t original_requests_part1;
    size_t original_invasions_part1;
    size_t start_funds_and_enemy_id;
    size_t map_size;
    size_t briefing;
    size_t original_requests_part2;
    size_t image;
    size_t herds;
    size_t original_demand_changes;
    size_t original_price_changes;
    size_t gladiator_revolt;
    size_t emperor_change;
    size_t random_events;
    size_t fishing;
    size_t original_requests_part3;
    size_t original_invasions_part2;
    size_t original_requests_part4;
    size_t rome_wheat;
    size_t allowed_buildings;
    size_t win_criteria;
    size_t map_points;
    size_t invasion_points;
    size_t misc;
    size_t alt_huts;
    size_t native_buildings;
    size_t introduction;
    size_t custom_variables;
    size_t custom_name;
    size_t end;

    // Cache info
    int version;
} buffer_offsets{};
auto calculate_buffer_offsets = [&](int scenario_version) {
    
    size_t next_start_offset = 0;

    if (scenario_version > SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.size = next_start_offset;
        next_start_offset = buffer_offsets.size + sizeof(int32_t);
    }

    buffer_offsets.start_info = next_start_offset;
    next_start_offset = buffer_offsets.start_info + 14;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part1 + 20 * 8;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part1 + 20 * 10;
    }

    buffer_offsets.start_funds_and_enemy_id = next_start_offset;
    next_start_offset = buffer_offsets.start_funds_and_enemy_id + 14;

    buffer_offsets.map_size = next_start_offset;
    next_start_offset = buffer_offsets.map_size + 16;

    buffer_offsets.briefing = next_start_offset;
    next_start_offset = buffer_offsets.briefing + 64 + 522;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part2 + 20;
    }

    buffer_offsets.image = next_start_offset;
    next_start_offset = buffer_offsets.image + 6;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 2;// favour reset monthly pattern is +2 bytes
    }
    buffer_offsets.herds = next_start_offset;
    next_start_offset = buffer_offsets.herds + 4 * 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_demand_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_demand_changes + 20 * 9;

        buffer_offsets.original_price_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_price_changes + 20 * 6;
    }

    buffer_offsets.gladiator_revolt = next_start_offset;
    next_start_offset = buffer_offsets.gladiator_revolt + 8;

    buffer_offsets.emperor_change = next_start_offset;
    next_start_offset = buffer_offsets.emperor_change + 8;

    buffer_offsets.random_events = next_start_offset;
    next_start_offset = buffer_offsets.random_events + 36;

    buffer_offsets.fishing = next_start_offset;
    next_start_offset = buffer_offsets.fishing + 8 * 4;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part3 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part3 + 20;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part2 + 20 * 1;
    }

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part4 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part4 + 20 * 4;
    }

    buffer_offsets.rome_wheat = next_start_offset;
    next_start_offset = buffer_offsets.rome_wheat + 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.allowed_buildings = next_start_offset;
        next_start_offset = buffer_offsets.allowed_buildings + 50 * 2;
    }

    buffer_offsets.win_criteria = next_start_offset;
    next_start_offset = buffer_offsets.win_criteria + 52;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 1;// earthkek pattern is +1 byte
    }
    buffer_offsets.map_points = next_start_offset;
    next_start_offset = buffer_offsets.map_points + 12;

    buffer_offsets.invasion_points = next_start_offset;
    next_start_offset = buffer_offsets.invasion_points + 8 * 4;

    buffer_offsets.misc = next_start_offset;
    next_start_offset = buffer_offsets.misc + 51;

    if (scenario_version > SCENARIO_LAST_NO_ALT_NATIVE_HUTS) {
        buffer_offsets.alt_huts = next_start_offset;
        next_start_offset = buffer_offsets.alt_huts + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        buffer_offsets.native_buildings = next_start_offset;
        next_start_offset = buffer_offsets.native_buildings + 12;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_MESSAGES) {
        buffer_offsets.introduction = next_start_offset;
        next_start_offset = buffer_offsets.introduction + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_VARIABLES && scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.custom_variables = next_start_offset;
        next_start_offset = buffer_offsets.custom_variables + 100 * 9;
    }

    buffer_offsets.custom_name = next_start_offset;
    next_start_offset = buffer_offsets.custom_name +
        (scenario_version > SCENARIO_LAST_WRONG_END_OFFSET ? 300 : 50);

    buffer_offsets.end = next_start_offset + 1;

    buffer_offsets.version = scenario_version;

};
    if (savegame_version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        return 1720;
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTENDED_REQUESTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTENDED_REQUESTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_MESSAGES);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_VARIABLES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_VARIABLES);
    } else if (savegame_version <= SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET) {
        calculate_buffer_offsets(SCENARIO_LAST_WRONG_END_OFFSET);
    } else if (savegame_version <= SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_STATIC_ORIGINAL_DATA);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_ALT_NATIVE_HUTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA);
    } else {
        calculate_buffer_offsets(SCENARIO_CURRENT_VERSION);
    }
    return (int) buffer_offsets.end;
}
static void get_version_data(savegame_version_data *version_data, int version, int resources, int foods)
{
    int multiplier = 1;
    int count_multiplier = 1;

    int total_new_resources = resources - 16;
    int total_new_food = foods - 7;

    version_data->piece_sizes.burning_totals = 8;
    if (version > SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION) {
        multiplier = 5;
    }
    if (version > SAVE_GAME_LAST_STATIC_VERSION) {
        multiplier = 0;
        version_data->piece_sizes.burning_totals = 4;
    }

    if (version > SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION) {
        count_multiplier = 0;
    }

    version_data->piece_sizes.image_grid = 52488 * (version > SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION ? 2 : 1);
    version_data->piece_sizes.building_grid = 52488 * (version > SAVE_GAME_LAST_U16_GRIDS ? 2 : 1);
    version_data->piece_sizes.terrain_grid = 52488 * (version > SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION ? 2 : 1);
    version_data->piece_sizes.rubble_grid = 104976;
    version_data->piece_sizes.figures = 128000 * multiplier;
    version_data->piece_sizes.route_figures = 1200 * multiplier;
    version_data->piece_sizes.route_paths = 300000 * multiplier;
    version_data->piece_sizes.formations = 6400 * multiplier;
    version_data->piece_sizes.buildings = 256000 * multiplier;
    version_data->piece_sizes.building_list_burning = 1000 * multiplier;
    version_data->piece_sizes.building_list_small = 1000 * multiplier;
    version_data->piece_sizes.building_list_large = 4000 * multiplier;
    version_data->piece_sizes.building_storages = 6400 * multiplier;
    version_data->piece_sizes.monument_deliveries = version > SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION ? 0 : 3200;
    version_data->piece_sizes.enemy_armies = version > SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG ? (125 * sizeof(int) * 9) : 900;
    version_data->piece_sizes.graph_order = version > SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA ? 4 : 8;
    // Due to an oversight, we only reduced the city_data buffer size when we added dynamic resources
    if (version <= SAVE_GAME_LAST_STATIC_RESOURCES) {
        version_data->piece_sizes.city_data = 36136;
    } else {
        version_data->piece_sizes.city_data = 11885;
        version_data->piece_sizes.city_data -= sizeof(int32_t) * 6 * 2;
        version_data->piece_sizes.city_data += total_new_resources * 18;
        version_data->piece_sizes.city_data += total_new_food * 4;
        if (version > SAVE_GAME_LAST_NO_FIXED_CITY_DATA_SIZE) {
            version_data->piece_sizes.city_data += 16 + 12 + 8 + 8;
        }
    }
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        // Bug - this should have acconted the new resource types, but since it hasn't before,
        // let's keep it to prevent crashes on opening. This is outdated anyway and was only available for unstable builds.
        version_data->piece_sizes.empire_cities = 2706;
    } else {
        version_data->piece_sizes.empire_cities = 0;
    }
    version_data->piece_sizes.trade_prices = 8 * resources;
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        version_data->piece_sizes.trade_route_limit = 20 * 4 * resources;
        version_data->piece_sizes.trade_route_traded = 20 * 4 * resources;
    } else {
        version_data->piece_sizes.trade_route_limit = 0;
        version_data->piece_sizes.trade_route_traded = 0;
    }
    version_data->piece_sizes.figure_traders = 1604 + 100 * 2 * resources;

    if (version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        version_data->piece_sizes.invasion_warnings = 0;
    } else {
        version_data->piece_sizes.invasion_warnings = 3232;
    }

    version_data->building_counts.culture1 = 132 * count_multiplier;
    version_data->building_counts.culture2 = 32 * count_multiplier;
    version_data->building_counts.culture3 = 40 * count_multiplier;
    version_data->building_counts.military = 16 * count_multiplier;
    version_data->building_counts.industry = 128 * count_multiplier;
    version_data->building_counts.support = 24 * count_multiplier;

    version_data->features.image_grid = version <= SAVE_GAME_LAST_STORED_IMAGE_IDS;
    version_data->features.monument_deliveries = version > SAVE_GAME_LAST_NO_DELIVERIES_VERSION;
    version_data->features.barracks_tower_sentry_request = version <= SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST;

    version_data->piece_sizes.scenario = scenario_size(version);
    if (version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        version_data->features.custom_empires = 0;
    } else {
        version_data->features.custom_empires = 1;
    }
    version_data->features.requests = version > SAVE_GAME_LAST_NO_EXTENDED_REQUESTS;

    if (version > SAVE_GAME_LAST_NO_EVENTS) {
        version_data->features.scenario_events = 1;
        version_data->features.scenario_conditions = 1;
        version_data->features.scenario_actions = 1;
        if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
            version_data->features.scenario_formulas = 1;
        } else {
            version_data->features.scenario_formulas = 0;
        }
        if (version > SAVE_GAME_LAST_NO_HOUSE_MODELS) {
            version_data->features.scenario_texts = 1;
        } else {
            version_data->features.scenario_texts = 0;
        }
    } else {
        version_data->features.scenario_events = 0;
        version_data->features.scenario_conditions = 0;
        version_data->features.scenario_actions = 0;
        version_data->features.scenario_formulas = 0;
        version_data->features.scenario_texts = 0;
    }

    if (version > SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        version_data->features.custom_messages_and_media = 1;
    } else {
        version_data->features.custom_messages_and_media = 0;
    }

    version_data->features.custom_empire_map_image = version > SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE;
    version_data->features.scenario_version = version > SAVE_GAME_LAST_NO_SCENARIO_VERSION;
    version_data->features.city_faction_info = version <= SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA;
    version_data->features.resource_version = version > SAVE_GAME_LAST_STATIC_RESOURCES;
    version_data->features.static_building_counts = version <= SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.visited_buildings = version > SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.custom_campaigns = version > SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS;
    version_data->features.dynamic_scenario_objects = version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA;
    version_data->features.custom_model_data = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.rubble_grid = version > SAVE_GAME_LAST_U16_GRIDS;
    version_data->features.custom_production_rates = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.monument_stages = version > SAVE_GAME_LAST_NO_HOUSE_MODELS;
}
template <typename Emit> static void visit(int version, int resources, int foods, Emit emit)
{


    savegame_version_data version_data{};
    get_version_data(&version_data, version, resources, foods);


    emit("scenario_campaign_mission", 4, 0);
    emit("file_version", 4, 0);
    if (version_data.features.resource_version) {
        emit("resource_version", 4, 0);
    }
    if (version_data.features.scenario_version) {
        emit("scenario_version", 4, 0);
    }
    if (version_data.features.image_grid) {
        emit("image_grid", version_data.piece_sizes.image_grid, 1);
    }
    emit("edge_grid", 26244, 1);
    emit("building_grid", version_data.piece_sizes.building_grid, 1);
    emit("terrain_grid", version_data.piece_sizes.terrain_grid, 1);
    emit("aqueduct_grid", 26244, 1);
    emit("figure_grid", 52488, 1);
    if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        emit("bitfields_grid", 52488, 1);
    } else {
        emit("bitfields_grid", 26244, 1);
    }
    emit("sprite_grid", 26244, 1);
    emit("random_grid", 26244, 0);
    emit("desirability_grid", 26244, 1);
    emit("elevation_grid", 26244, 1);
    emit("building_damage_grid", 26244, 1);
    emit("aqueduct_backup_grid", 26244, 1);
    emit("sprite_backup_grid", 26244, 1);
    emit("figures", version_data.piece_sizes.figures, 1);
    emit("route_figures", version_data.piece_sizes.route_figures, 1);
    emit("route_paths", version_data.piece_sizes.route_paths, 1);
    emit("formations", version_data.piece_sizes.formations, 1);
    emit("formation_totals", 12, 0);
    emit("city_data", version_data.piece_sizes.city_data, 1);
    if (version_data.features.city_faction_info) {
        emit("city_faction_unknown", 2, 0);
    }
    emit("player_name", 64, 0);
    if (version_data.features.city_faction_info) {
        emit("city_faction", 4, 0);
    }
    emit("buildings", version_data.piece_sizes.buildings, 1);
    emit("city_view_orientation", 4, 0);
    emit("game_time", 20, 0);
    emit("building_extra_highest_id_ever", 8, 0);
    emit("random_iv", 8, 0);
    emit("city_view_camera", 8, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture1", version_data.building_counts.culture1, 0);
    }
    emit("city_graph_order", version_data.piece_sizes.graph_order, 0);
    emit("emperor_change_time", 8, 0);
    emit("empire", 12, 0);
    if (version_data.features.custom_empire_map_image) {
        emit("empire_map", 0, 0);
    }
    emit("empire_cities", version_data.piece_sizes.empire_cities, 1);
    if (version_data.features.static_building_counts) {
        emit("building_count_industry", version_data.building_counts.industry, 0);
    }
    emit("trade_prices", version_data.piece_sizes.trade_prices, 0);
    emit("figure_names", 84, 0);
    emit("culture_coverage", 60, 0);
    emit("scenario", version_data.piece_sizes.scenario, 0);
    if (version_data.features.requests) {
        emit("requests", 0, 0);
    }
    if (version_data.features.dynamic_scenario_objects) {
        emit("invasions", 0, 1);
        emit("demand_changes", 0, 1);
        emit("price_changes", 0, 1);
        emit("allowed_buildings", 0, 1);
        emit("custom_variables", 0, 1);
    }
    if (version_data.features.scenario_events) {
        emit("scenario_events", 0, 0);
    }
    if (version_data.features.scenario_formulas) {
        emit("scenario_formulas", 0, 0);
    }
    if (version_data.features.scenario_texts) {
        emit("scenario_texts", 0, 0);
    }
    if (version_data.features.scenario_conditions) {
        emit("scenario_conditions", 0, 0);
    }
    if (version_data.features.scenario_actions) {
        emit("scenario_actions", 0, 0);
    }
    if (version_data.features.custom_messages_and_media) {
        emit("custom_messages", 0, 0);
        emit("custom_media", 0, 0);
        emit("message_media_text_blob", 0, 0);
        emit("message_media_metadata", 0, 0);
    }
    if (version_data.features.custom_model_data) {
        emit("model_data", 0, 0);
    }
    emit("max_game_year", 4, 0);
    emit("earthquake", 60, 0);
    emit("emperor_change_state", 4, 0);
    emit("messages", 16000, 1);
    emit("message_extra", 12, 0);
    emit("population_messages", 10, 0);
    emit("message_counts", 80, 0);
    emit("message_delays", 80, 0);
    emit("building_list_burning_totals", version_data.piece_sizes.burning_totals, 0);
    emit("figure_sequence", 4, 0);
    emit("scenario_settings", 12, 0);
    emit("invasion_warnings", version_data.piece_sizes.invasion_warnings, 1);
    emit("scenario_is_custom", 4, 0);
    emit("city_sounds", 8960, 0);
    emit("building_extra_highest_id", 4, 0);
    emit("figure_traders", version_data.piece_sizes.figure_traders, 0);
    emit("building_list_burning", version_data.piece_sizes.building_list_burning, 1);
    emit("building_list_small", version_data.piece_sizes.building_list_small, 1);
    emit("building_list_large", version_data.piece_sizes.building_list_large, 1);
    emit("tutorial_part1", 32, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_military", version_data.building_counts.military, 0);
    }
    emit("enemy_army_totals", 20, 0);
    emit("building_storages", version_data.piece_sizes.building_storages, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture2", version_data.building_counts.culture2, 0);
        emit("building_count_support", version_data.building_counts.support, 0);
    }
    emit("tutorial_part2", 4, 0);
    emit("gladiator_revolt", 16, 0);
    if (version > SAVE_GAME_LAST_NO_EMPIRE_EDITOR) {
        emit("trade_routes", 0, 1);
        if (version > SAVE_GAME_LAST_NO_LEDGER) {
            emit("trade_history", 0, 1);
        }
    } else {
        emit("trade_route_limit", version_data.piece_sizes.trade_route_limit, 1);
        emit("trade_route_traded", version_data.piece_sizes.trade_route_traded, 1);
    }
    if (version_data.features.barracks_tower_sentry_request) {
        emit("building_barracks_tower_sentry", 4, 0);
    }
    emit("building_extra_sequence", 4, 0);
    emit("routing_counters", 16, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture3", version_data.building_counts.culture3, 0);
    }
    emit("enemy_armies", version_data.piece_sizes.enemy_armies, 0);
    emit("city_entry_exit_xy", 16, 0);
    emit("last_invasion_id", 2, 0);
    emit("building_extra_corrupt_houses", 8, 0);
    emit("scenario_name", 65, 0);
    emit("bookmarks", 32, 0);
    emit("tutorial_part3", 4, 0);
    emit("city_entry_exit_grid_offset", 8, 0);
    if (version_data.features.custom_campaigns) {
        emit("campaign_name", 0, 0);
    }
    emit("end_marker", 284, 0); // 71x 4-bytes emptiness
    if (version_data.features.monument_deliveries) {
        emit("deliveries", version_data.piece_sizes.monument_deliveries, 0);
    }
    if (version_data.features.custom_empires) {
        emit("custom_empire", 0, 1);
    }
    if (version_data.features.visited_buildings) {
        emit("visited_buildings", 0, 1);
    }
    if (version_data.features.rubble_grid) {
        emit("rubble_grid", 104976, 1);
    }
    if (version_data.features.custom_production_rates) {
        emit("production_rates", 0, 1);
    }
    if (version_data.features.monument_stages) {
        emit("monument_stages", 0, 1);
    }
    if (version > SAVE_GAME_LAST_NO_LEDGER) {
        emit("finance_ledger", 0, 0);
    }
}
}


// Producer aa9f31ab4ea2c4a8cfacd75a379ec3f29f9593c2; layout 3b04b3fff5c4a5260615e6d33d81c41438b37fdc7bbc3f0ee2ce67f050f94dbc
namespace layout_4 {
enum {

    SAVE_GAME_CURRENT_VERSION = 0xbd,

    SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION = 0x66,
    SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION = 0x76,
    SAVE_GAME_LAST_NO_DELIVERIES_VERSION = 0x77,
    SAVE_GAME_LAST_STATIC_VERSION = 0x78,
    SAVE_GAME_LAST_JOINED_IMPORT_EXPORT_VERSION = 0x79,
    SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION = 0x80,
    SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION = 0x81,
    SAVE_GAME_LAST_STORED_IMAGE_IDS = 0x83,
    // SAVE_GAME_INCREASE_GRANARY_CAPACITY shall be updated if we decide to change granary capacity again.
    SAVE_GAME_INCREASE_GRANARY_CAPACITY = 0x85,
    SAVE_GAME_ROADBLOCK_DATA_MOVED_FROM_SUBTYPE = 0x86,
    SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION = 0x86,
    SAVE_GAME_LAST_CARAVANSERAI_WRONG_OFFSET = 0x87,
    SAVE_GAME_LAST_ZIP_COMPRESSION = 0x88,
    SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG = 0x89,
    SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST = 0x8a,
    // SAVE_GAME_LAST_WITHOUT_HIGHWAYS = 0x8b, no actual changes to how games are saved. Crudelios just wants this here
    SAVE_GAME_LAST_UNVERSIONED_SCENARIOS = 0x8c,
    SAVE_GAME_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 0x8d,
    // the difference between this version and UNVERSIONED_SCENARIOS above is this one actually saves the scenario version
    // in the data, whereas the previous one did a lookup based on the save version
    SAVE_GAME_LAST_NO_SCENARIO_VERSION = 0x8e,
    SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA = 0x8f,
    SAVE_GAME_LAST_STATIC_RESOURCES = 0x90,
    SAVE_GAME_LAST_GLOBAL_BUILDING_INFO = 0x91,
    SAVE_GAME_LAST_NO_GOLD_AND_MINTING = 0x92,
    SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS = 0x93,
    SAVE_GAME_LAST_NO_EXTENDED_REQUESTS = 0x94,
    SAVE_GAME_LAST_NO_EVENTS = 0x95,
    SAVE_GAME_LAST_NO_CUSTOM_MESSAGES = 0x96,
    SAVE_GAME_LAST_NO_CART_DEPOT = 0x97,
    SAVE_GAME_LAST_NO_NEW_MONUMENT_RESOURCES = 0x98,
    SAVE_GAME_LAST_MONUMENT_TYPE_DATA = 0x99,
    SAVE_GAME_LAST_NO_CUSTOM_VARIABLES = 0x9a,
    SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET = 0x9b,
    SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 0x9c,
    SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS = 0x9d,
    SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA = 0x9e,
    SAVE_GAME_LAST_NO_LATRINES = 0x9f,
    SAVE_GAME_LAST_SPRITE_BRIDGES = 0xa0,
    SAVE_GAME_LAST_SPRITE_BRIDGES_MIGRATION_FIX = 0xa1,
    SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS = 0xa2,
    SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS = 0xa3,
    SAVE_GAME_LAST_STORAGE_STATE_AND_QUANTITY_TOGETHER = 0xa4,
    SAVE_GAME_LAST_10_LEGIONS_MAX = 0xa5,
    SAVE_GAME_LAST_GRANARY_WAREHOUSE_NON_ROADBLOCKS = 0xa6,
    SAVE_GAME_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 0xa7,
    SAVE_GAME_LAST_U16_GRIDS = 0xa8,
    SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA = 0xa9,
    SAVE_GAME_LAST_STATIC_PATHS_AND_ROUTES = 0xaa,
    SAVE_GAME_LAST_NO_EMPIRE_EDITOR = 0xab,
    SAVE_GAME_LAST_LIMITED_ROUTE_COST = 0xac,
    SAVE_GAME_LAST_NO_WATER_IN_ADVISORS = 0xad,
    SAVE_GAME_LAST_NO_SHARED_BUILDINGS = 0xae,
    SAVE_GAME_LAST_NO_PLEBIANS = 0xaf,
    SAVE_GAME_LAST_NO_DOG = 0xb0,
    SAVE_GAME_LAST_ALTAR_ROTATION_FIX = 0xb1,
    SAVE_GAME_LAST_NO_HIGHWAY_STATION = 0xb2,
    SAVE_GAME_LAST_NO_TRIUMPHAL_ARCH_MONUMENT = 0xb3,
    SAVE_GAME_LAST_NO_FORT_ORIENTATION = 0xb4,
    SAVE_GAME_LAST_NO_LEDGER = 0xb5, // only existed in test versions, never on master.
    SAVE_GAME_LAST_NO_FINANCE_OVERVIEW_HISTORY = 0xb6,
    SAVE_GAME_LAST_NO_HOUSE_MODELS = 0xb7,
    SAVE_GAME_LAST_NO_FIXED_FOURTH_RELIGIONS = 0xb8,
    SAVE_GAME_LAST_NO_FIXED_CITY_DATA_SIZE = 0xb9,
    SAVE_GAME_LAST_NO_BUFFER_SIZE_IN_MODEL_DATA = 0xba,
    SAVE_GAME_LAST_NO_WILLOW_TREE = 0xbb,
    SAVE_GAME_LAST_NO_SHALLOWS = 0xbc
};
enum {
    SCENARIO_CURRENT_VERSION = 25,

    SCENARIO_VERSION_NONE = 0,
    SCENARIO_LAST_UNVERSIONED = 1,
    SCENARIO_LAST_NO_WAGE_LIMITS = 2,
    SCENARIO_LAST_EMPIRE_OBJECT_BUFFERS = 3,
    SCENARIO_LAST_EMPIRE_RESOURCES_U8 = 4,
    SCENARIO_LAST_EMPIRE_RESOURCES_ALWAYS_WRITE = 5,
    SCENARIO_LAST_NO_SAVE_VERSION_WRITE = 6,
    SCENARIO_LAST_NO_STATIC_RESOURCES = 7,
    SCENARIO_LAST_NO_DYNAMIC_OBJECTS = 8,
    SCENARIO_LAST_NO_EXTENDED_REQUESTS = 9,
    SCENARIO_LAST_NO_EVENTS = 10,
    SCENARIO_LAST_NO_CUSTOM_MESSAGES = 11,
    SCENARIO_LAST_NO_CUSTOM_VARIABLES = 12,
    SCENARIO_LAST_WRONG_END_OFFSET = 13,
    SCENARIO_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE = 14,
    SCENARIO_LAST_STATIC_ORIGINAL_DATA = 15,
    SCENARIO_LAST_NO_ALT_NATIVE_HUTS = 16,
    SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS = 17,
    SCENARIO_LAST_NO_VISIBLE_CUSTOM_VARIABLES = 18,
    SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA = 19,
    SCENARIO_LAST_NO_EMPIRE_EDITOR = 20,
    SCENARIO_LAST_LIMITED_ROUTE_COST = 21,
    SCENARIO_LAST_NO_HOUSE_MODELS = 22,
    SCENARIO_LAST_NO_BUFFER_SIZE_IN_MODEL_DATA = 23,
    SCENARIO_LAST_NO_WILLOW_TREE = 24,
    SCENARIO_LAST_LEGACY_TERRAIN = 25
};
enum {
    SAVEGAME_STATUS_NEWER_VERSION = -1,
    SAVEGAME_STATUS_INVALID = 0,
    SAVEGAME_STATUS_OK = 1
};
typedef struct {
    struct {
        int burning_totals;
        int image_grid;
        int building_grid;
        int rubble_grid;
        int terrain_grid;
        int figures;
        int route_figures;
        int route_paths;
        int formations;
        int buildings;
        int building_list_burning;
        int building_list_small;
        int building_list_large;
        int building_storages;
        int monument_deliveries;
        int enemy_armies;
        int scenario;
        int graph_order;
        int city_data;
        int empire_cities;
        int trade_prices;
        int trade_route_limit;
        int trade_route_traded;
        int figure_traders;
        int invasion_warnings;
    } piece_sizes;
    struct {
        int culture1;
        int culture2;
        int culture3;
        int military;
        int industry;
        int support;
    } building_counts;
    struct {
        int image_grid;
        int monument_deliveries;
        int barracks_tower_sentry_request;
        int custom_empires;
        int custom_empire_map_image;
        int scenario_version;
        int requests;
        int scenario_events;
        int scenario_conditions;
        int scenario_actions;
        int scenario_formulas;
        int scenario_texts;
        int custom_messages_and_media;
        int city_faction_info;
        int resource_version;
        int static_building_counts;
        int visited_buildings;
        int custom_campaigns;
        int dynamic_scenario_objects;
        int custom_model_data;
        int rubble_grid;
        int custom_production_rates;
        int monument_stages;
    } features;
} savegame_version_data;
static int scenario_size(int savegame_version)
{
struct {
    size_t size;
    size_t start_info;
    size_t original_requests_part1;
    size_t original_invasions_part1;
    size_t start_funds_and_enemy_id;
    size_t map_size;
    size_t briefing;
    size_t original_requests_part2;
    size_t image;
    size_t herds;
    size_t original_demand_changes;
    size_t original_price_changes;
    size_t gladiator_revolt;
    size_t emperor_change;
    size_t random_events;
    size_t fishing;
    size_t original_requests_part3;
    size_t original_invasions_part2;
    size_t original_requests_part4;
    size_t rome_wheat;
    size_t allowed_buildings;
    size_t win_criteria;
    size_t map_points;
    size_t invasion_points;
    size_t misc;
    size_t alt_huts;
    size_t native_buildings;
    size_t introduction;
    size_t custom_variables;
    size_t custom_name;
    size_t end;

    // Cache info
    int version;
} buffer_offsets{};
auto calculate_buffer_offsets = [&](int scenario_version) {
    
    size_t next_start_offset = 0;

    if (scenario_version > SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.size = next_start_offset;
        next_start_offset = buffer_offsets.size + sizeof(int32_t);
    }

    buffer_offsets.start_info = next_start_offset;
    next_start_offset = buffer_offsets.start_info + 14;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part1 + 20 * 8;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part1 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part1 + 20 * 10;
    }

    buffer_offsets.start_funds_and_enemy_id = next_start_offset;
    next_start_offset = buffer_offsets.start_funds_and_enemy_id + 14;

    buffer_offsets.map_size = next_start_offset;
    next_start_offset = buffer_offsets.map_size + 16;

    buffer_offsets.briefing = next_start_offset;
    next_start_offset = buffer_offsets.briefing + 64 + 522;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part2 + 20;
    }

    buffer_offsets.image = next_start_offset;
    next_start_offset = buffer_offsets.image + 6;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 2;// favour reset monthly pattern is +2 bytes
    }
    buffer_offsets.herds = next_start_offset;
    next_start_offset = buffer_offsets.herds + 4 * 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_demand_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_demand_changes + 20 * 9;

        buffer_offsets.original_price_changes = next_start_offset;
        next_start_offset = buffer_offsets.original_price_changes + 20 * 6;
    }

    buffer_offsets.gladiator_revolt = next_start_offset;
    next_start_offset = buffer_offsets.gladiator_revolt + 8;

    buffer_offsets.emperor_change = next_start_offset;
    next_start_offset = buffer_offsets.emperor_change + 8;

    buffer_offsets.random_events = next_start_offset;
    next_start_offset = buffer_offsets.random_events + 36;

    buffer_offsets.fishing = next_start_offset;
    next_start_offset = buffer_offsets.fishing + 8 * 4;

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part3 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part3 + 20;
    }

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.original_invasions_part2 = next_start_offset;
        next_start_offset = buffer_offsets.original_invasions_part2 + 20 * 1;
    }

    if (scenario_version <= SCENARIO_LAST_NO_EXTENDED_REQUESTS) {
        buffer_offsets.original_requests_part4 = next_start_offset;
        next_start_offset = buffer_offsets.original_requests_part4 + 20 * 4;
    }

    buffer_offsets.rome_wheat = next_start_offset;
    next_start_offset = buffer_offsets.rome_wheat + 4;

    if (scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.allowed_buildings = next_start_offset;
        next_start_offset = buffer_offsets.allowed_buildings + 50 * 2;
    }

    buffer_offsets.win_criteria = next_start_offset;
    next_start_offset = buffer_offsets.win_criteria + 52;
    if (scenario_version > SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        next_start_offset += 1;// earthkek pattern is +1 byte
    }
    buffer_offsets.map_points = next_start_offset;
    next_start_offset = buffer_offsets.map_points + 12;

    buffer_offsets.invasion_points = next_start_offset;
    next_start_offset = buffer_offsets.invasion_points + 8 * 4;

    buffer_offsets.misc = next_start_offset;
    next_start_offset = buffer_offsets.misc + 51;

    if (scenario_version > SCENARIO_LAST_NO_ALT_NATIVE_HUTS) {
        buffer_offsets.alt_huts = next_start_offset;
        next_start_offset = buffer_offsets.alt_huts + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        buffer_offsets.native_buildings = next_start_offset;
        next_start_offset = buffer_offsets.native_buildings + 12;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_MESSAGES) {
        buffer_offsets.introduction = next_start_offset;
        next_start_offset = buffer_offsets.introduction + 4;
    }

    if (scenario_version > SCENARIO_LAST_NO_CUSTOM_VARIABLES && scenario_version <= SCENARIO_LAST_STATIC_ORIGINAL_DATA) {
        buffer_offsets.custom_variables = next_start_offset;
        next_start_offset = buffer_offsets.custom_variables + 100 * 9;
    }

    buffer_offsets.custom_name = next_start_offset;
    next_start_offset = buffer_offsets.custom_name +
        (scenario_version > SCENARIO_LAST_WRONG_END_OFFSET ? 300 : 50);

    buffer_offsets.end = next_start_offset + 1;

    buffer_offsets.version = scenario_version;

};
    if (savegame_version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        return 1720;
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTENDED_REQUESTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTENDED_REQUESTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_MESSAGES);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_CUSTOM_VARIABLES) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_CUSTOM_VARIABLES);
    } else if (savegame_version <= SAVE_GAME_LAST_WRONG_SCENARIO_END_OFFSET) {
        calculate_buffer_offsets(SCENARIO_LAST_WRONG_END_OFFSET);
    } else if (savegame_version <= SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_STATIC_ORIGINAL_DATA);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_ALT_NATIVE_HUTS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_ALT_NATIVE_HUTS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_EXTRA_NATIVE_BUILDINGS) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_EXTRA_NATIVE_BUILDINGS);
    } else if (savegame_version <= SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        calculate_buffer_offsets(SCENARIO_LAST_NO_FORMULAS_AND_MODEL_DATA);
    } else {
        calculate_buffer_offsets(SCENARIO_CURRENT_VERSION);
    }
    return (int) buffer_offsets.end;
}
static void get_version_data(savegame_version_data *version_data, int version, int resources, int foods)
{
    int multiplier = 1;
    int count_multiplier = 1;

    int total_new_resources = resources - 16;
    int total_new_food = foods - 7;

    version_data->piece_sizes.burning_totals = 8;
    if (version > SAVE_GAME_LAST_ORIGINAL_LIMITS_VERSION) {
        multiplier = 5;
    }
    if (version > SAVE_GAME_LAST_STATIC_VERSION) {
        multiplier = 0;
        version_data->piece_sizes.burning_totals = 4;
    }

    if (version > SAVE_GAME_LAST_STATIC_BUILDING_COUNT_VERSION) {
        count_multiplier = 0;
    }

    version_data->piece_sizes.image_grid = 52488 * (version > SAVE_GAME_LAST_SMALLER_IMAGE_ID_VERSION ? 2 : 1);
    version_data->piece_sizes.building_grid = 52488 * (version > SAVE_GAME_LAST_U16_GRIDS ? 2 : 1);
    version_data->piece_sizes.terrain_grid = 52488 * (version > SAVE_GAME_LAST_ORIGINAL_TERRAIN_DATA_SIZE_VERSION ? 2 : 1);
    version_data->piece_sizes.rubble_grid = 104976;
    version_data->piece_sizes.figures = 128000 * multiplier;
    version_data->piece_sizes.route_figures = 1200 * multiplier;
    version_data->piece_sizes.route_paths = 300000 * multiplier;
    version_data->piece_sizes.formations = 6400 * multiplier;
    version_data->piece_sizes.buildings = 256000 * multiplier;
    version_data->piece_sizes.building_list_burning = 1000 * multiplier;
    version_data->piece_sizes.building_list_small = 1000 * multiplier;
    version_data->piece_sizes.building_list_large = 4000 * multiplier;
    version_data->piece_sizes.building_storages = 6400 * multiplier;
    version_data->piece_sizes.monument_deliveries = version > SAVE_GAME_LAST_STATIC_MONUMENT_DELIVERIES_VERSION ? 0 : 3200;
    version_data->piece_sizes.enemy_armies = version > SAVE_GAME_LAST_ENEMY_ARMIES_BUFFER_BUG ? (125 * sizeof(int) * 9) : 900;
    version_data->piece_sizes.graph_order = version > SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA ? 4 : 8;
    // Due to an oversight, we only reduced the city_data buffer size when we added dynamic resources
    if (version <= SAVE_GAME_LAST_STATIC_RESOURCES) {
        version_data->piece_sizes.city_data = 36136;
    } else {
        version_data->piece_sizes.city_data = 11885;
        version_data->piece_sizes.city_data -= sizeof(int32_t) * 6 * 2;
        version_data->piece_sizes.city_data += total_new_resources * 18;
        version_data->piece_sizes.city_data += total_new_food * 4;
        if (version > SAVE_GAME_LAST_NO_FIXED_CITY_DATA_SIZE) {
            version_data->piece_sizes.city_data += 16 + 12 + 8 + 8;
        }
    }
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        // Bug - this should have acconted the new resource types, but since it hasn't before,
        // let's keep it to prevent crashes on opening. This is outdated anyway and was only available for unstable builds.
        version_data->piece_sizes.empire_cities = 2706;
    } else {
        version_data->piece_sizes.empire_cities = 0;
    }
    version_data->piece_sizes.trade_prices = 8 * resources;
    if (version <= SAVE_GAME_LAST_STATIC_SCENARIO_OBJECTS) {
        version_data->piece_sizes.trade_route_limit = 20 * 4 * resources;
        version_data->piece_sizes.trade_route_traded = 20 * 4 * resources;
    } else {
        version_data->piece_sizes.trade_route_limit = 0;
        version_data->piece_sizes.trade_route_traded = 0;
    }
    version_data->piece_sizes.figure_traders = 1604 + 100 * 2 * resources;

    if (version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA) {
        version_data->piece_sizes.invasion_warnings = 0;
    } else {
        version_data->piece_sizes.invasion_warnings = 3232;
    }

    version_data->building_counts.culture1 = 132 * count_multiplier;
    version_data->building_counts.culture2 = 32 * count_multiplier;
    version_data->building_counts.culture3 = 40 * count_multiplier;
    version_data->building_counts.military = 16 * count_multiplier;
    version_data->building_counts.industry = 128 * count_multiplier;
    version_data->building_counts.support = 24 * count_multiplier;

    version_data->features.image_grid = version <= SAVE_GAME_LAST_STORED_IMAGE_IDS;
    version_data->features.monument_deliveries = version > SAVE_GAME_LAST_NO_DELIVERIES_VERSION;
    version_data->features.barracks_tower_sentry_request = version <= SAVE_GAME_LAST_BARRACKS_TOWER_SENTRY_REQUEST;

    version_data->piece_sizes.scenario = scenario_size(version);
    if (version <= SAVE_GAME_LAST_UNVERSIONED_SCENARIOS) {
        version_data->features.custom_empires = 0;
    } else {
        version_data->features.custom_empires = 1;
    }
    version_data->features.requests = version > SAVE_GAME_LAST_NO_EXTENDED_REQUESTS;

    if (version > SAVE_GAME_LAST_NO_EVENTS) {
        version_data->features.scenario_events = 1;
        version_data->features.scenario_conditions = 1;
        version_data->features.scenario_actions = 1;
        if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
            version_data->features.scenario_formulas = 1;
        } else {
            version_data->features.scenario_formulas = 0;
        }
        if (version > SAVE_GAME_LAST_NO_HOUSE_MODELS) {
            version_data->features.scenario_texts = 1;
        } else {
            version_data->features.scenario_texts = 0;
        }
    } else {
        version_data->features.scenario_events = 0;
        version_data->features.scenario_conditions = 0;
        version_data->features.scenario_actions = 0;
        version_data->features.scenario_formulas = 0;
        version_data->features.scenario_texts = 0;
    }

    if (version > SAVE_GAME_LAST_NO_CUSTOM_MESSAGES) {
        version_data->features.custom_messages_and_media = 1;
    } else {
        version_data->features.custom_messages_and_media = 0;
    }

    version_data->features.custom_empire_map_image = version > SAVE_GAME_LAST_NO_CUSTOM_EMPIRE_MAP_IMAGE;
    version_data->features.scenario_version = version > SAVE_GAME_LAST_NO_SCENARIO_VERSION;
    version_data->features.city_faction_info = version <= SAVE_GAME_LAST_UNKNOWN_UNUSED_CITY_DATA;
    version_data->features.resource_version = version > SAVE_GAME_LAST_STATIC_RESOURCES;
    version_data->features.static_building_counts = version <= SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.visited_buildings = version > SAVE_GAME_LAST_GLOBAL_BUILDING_INFO;
    version_data->features.custom_campaigns = version > SAVE_GAME_LAST_NO_CUSTOM_CAMPAIGNS;
    version_data->features.dynamic_scenario_objects = version > SAVE_GAME_LAST_STATIC_SCENARIO_ORIGINAL_DATA;
    version_data->features.custom_model_data = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.rubble_grid = version > SAVE_GAME_LAST_U16_GRIDS;
    version_data->features.custom_production_rates = version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA;
    version_data->features.monument_stages = version > SAVE_GAME_LAST_NO_HOUSE_MODELS;
}
template <typename Emit> static void visit(int version, int resources, int foods, Emit emit)
{


    savegame_version_data version_data{};
    get_version_data(&version_data, version, resources, foods);


    emit("scenario_campaign_mission", 4, 0);
    emit("file_version", 4, 0);
    if (version_data.features.resource_version) {
        emit("resource_version", 4, 0);
    }
    if (version_data.features.scenario_version) {
        emit("scenario_version", 4, 0);
    }
    if (version_data.features.image_grid) {
        emit("image_grid", version_data.piece_sizes.image_grid, 1);
    }
    emit("edge_grid", 26244, 1);
    emit("building_grid", version_data.piece_sizes.building_grid, 1);
    emit("terrain_grid", version_data.piece_sizes.terrain_grid, 1);
    emit("aqueduct_grid", 26244, 1);
    emit("figure_grid", 52488, 1);
    if (version > SAVE_GAME_LAST_NO_FORMULAS_AND_MODEL_DATA) {
        emit("bitfields_grid", 52488, 1);
    } else {
        emit("bitfields_grid", 26244, 1);
    }
    emit("sprite_grid", 26244, 1);
    emit("random_grid", 26244, 0);
    emit("desirability_grid", 26244, 1);
    emit("elevation_grid", 26244, 1);
    emit("building_damage_grid", 26244, 1);
    emit("aqueduct_backup_grid", 26244, 1);
    emit("sprite_backup_grid", 26244, 1);
    emit("figures", version_data.piece_sizes.figures, 1);
    emit("route_figures", version_data.piece_sizes.route_figures, 1);
    emit("route_paths", version_data.piece_sizes.route_paths, 1);
    emit("formations", version_data.piece_sizes.formations, 1);
    emit("formation_totals", 12, 0);
    emit("city_data", version_data.piece_sizes.city_data, 1);
    if (version_data.features.city_faction_info) {
        emit("city_faction_unknown", 2, 0);
    }
    emit("player_name", 64, 0);
    if (version_data.features.city_faction_info) {
        emit("city_faction", 4, 0);
    }
    emit("buildings", version_data.piece_sizes.buildings, 1);
    emit("city_view_orientation", 4, 0);
    emit("game_time", 20, 0);
    emit("building_extra_highest_id_ever", 8, 0);
    emit("random_iv", 8, 0);
    emit("city_view_camera", 8, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture1", version_data.building_counts.culture1, 0);
    }
    emit("city_graph_order", version_data.piece_sizes.graph_order, 0);
    emit("emperor_change_time", 8, 0);
    emit("empire", 12, 0);
    if (version_data.features.custom_empire_map_image) {
        emit("empire_map", 0, 0);
    }
    emit("empire_cities", version_data.piece_sizes.empire_cities, 1);
    if (version_data.features.static_building_counts) {
        emit("building_count_industry", version_data.building_counts.industry, 0);
    }
    emit("trade_prices", version_data.piece_sizes.trade_prices, 0);
    emit("figure_names", 84, 0);
    emit("culture_coverage", 60, 0);
    emit("scenario", version_data.piece_sizes.scenario, 0);
    if (version_data.features.requests) {
        emit("requests", 0, 0);
    }
    if (version_data.features.dynamic_scenario_objects) {
        emit("invasions", 0, 1);
        emit("demand_changes", 0, 1);
        emit("price_changes", 0, 1);
        emit("allowed_buildings", 0, 1);
        emit("custom_variables", 0, 1);
    }
    if (version_data.features.scenario_events) {
        emit("scenario_events", 0, 0);
    }
    if (version_data.features.scenario_formulas) {
        emit("scenario_formulas", 0, 0);
    }
    if (version_data.features.scenario_texts) {
        emit("scenario_texts", 0, 0);
    }
    if (version_data.features.scenario_conditions) {
        emit("scenario_conditions", 0, 0);
    }
    if (version_data.features.scenario_actions) {
        emit("scenario_actions", 0, 0);
    }
    if (version_data.features.custom_messages_and_media) {
        emit("custom_messages", 0, 0);
        emit("custom_media", 0, 0);
        emit("message_media_text_blob", 0, 0);
        emit("message_media_metadata", 0, 0);
    }
    if (version_data.features.custom_model_data) {
        emit("model_data", 0, 0);
    }
    emit("max_game_year", 4, 0);
    emit("earthquake", 60, 0);
    emit("emperor_change_state", 4, 0);
    emit("messages", 16000, 1);
    emit("message_extra", 12, 0);
    emit("population_messages", 10, 0);
    emit("message_counts", 80, 0);
    emit("message_delays", 80, 0);
    emit("building_list_burning_totals", version_data.piece_sizes.burning_totals, 0);
    emit("figure_sequence", 4, 0);
    emit("scenario_settings", 12, 0);
    emit("invasion_warnings", version_data.piece_sizes.invasion_warnings, 1);
    emit("scenario_is_custom", 4, 0);
    emit("city_sounds", 8960, 0);
    emit("building_extra_highest_id", 4, 0);
    emit("figure_traders", version_data.piece_sizes.figure_traders, 0);
    emit("building_list_burning", version_data.piece_sizes.building_list_burning, 1);
    emit("building_list_small", version_data.piece_sizes.building_list_small, 1);
    emit("building_list_large", version_data.piece_sizes.building_list_large, 1);
    emit("tutorial_part1", 32, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_military", version_data.building_counts.military, 0);
    }
    emit("enemy_army_totals", 20, 0);
    emit("building_storages", version_data.piece_sizes.building_storages, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture2", version_data.building_counts.culture2, 0);
        emit("building_count_support", version_data.building_counts.support, 0);
    }
    emit("tutorial_part2", 4, 0);
    emit("gladiator_revolt", 16, 0);
    if (version > SAVE_GAME_LAST_NO_EMPIRE_EDITOR) {
        emit("trade_routes", 0, 1);
        if (version > SAVE_GAME_LAST_NO_LEDGER) {
            emit("trade_history", 0, 1);
        }
    } else {
        emit("trade_route_limit", version_data.piece_sizes.trade_route_limit, 1);
        emit("trade_route_traded", version_data.piece_sizes.trade_route_traded, 1);
    }
    if (version_data.features.barracks_tower_sentry_request) {
        emit("building_barracks_tower_sentry", 4, 0);
    }
    emit("building_extra_sequence", 4, 0);
    emit("routing_counters", 16, 0);
    if (version_data.features.static_building_counts) {
        emit("building_count_culture3", version_data.building_counts.culture3, 0);
    }
    emit("enemy_armies", version_data.piece_sizes.enemy_armies, 0);
    emit("city_entry_exit_xy", 16, 0);
    emit("last_invasion_id", 2, 0);
    emit("building_extra_corrupt_houses", 8, 0);
    emit("scenario_name", 65, 0);
    emit("bookmarks", 32, 0);
    emit("tutorial_part3", 4, 0);
    emit("city_entry_exit_grid_offset", 8, 0);
    if (version_data.features.custom_campaigns) {
        emit("campaign_name", 0, 0);
    }
    emit("end_marker", 284, 0); // 71x 4-bytes emptiness
    if (version_data.features.monument_deliveries) {
        emit("deliveries", version_data.piece_sizes.monument_deliveries, 0);
    }
    if (version_data.features.custom_empires) {
        emit("custom_empire", 0, 1);
    }
    if (version_data.features.visited_buildings) {
        emit("visited_buildings", 0, 1);
    }
    if (version_data.features.rubble_grid) {
        emit("rubble_grid", 104976, 1);
    }
    if (version_data.features.custom_production_rates) {
        emit("production_rates", 0, 1);
    }
    if (version_data.features.monument_stages) {
        emit("monument_stages", 0, 1);
    }
    if (version > SAVE_GAME_LAST_NO_LEDGER) {
        emit("finance_ledger", 0, 0);
    }
}
}

} // namespace augustus_archive_layouts
